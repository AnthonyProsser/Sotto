# Sotto — Decisions log

**Newest first. Append, never rewrite.**

## How this file works

`docs/` holds read-only copies of the spec, the build order, and the token sheet. The originals live in `/Users/anthonyprosser/Documents/Sotto/`. Nothing in `docs/` is ever edited here — see `CLAUDE.md` §0.

Decisions get made during build sessions, because a spec that survives contact with Swift unchanged was not a spec. When one happens:

1. **Claude Code appends an entry below** — newest at the top of the table, using the template.
2. **Claude Code says so in its reply**, naming the spec section it contradicts if there is one. A decision logged silently is a decision nobody folds back.
3. **Anthony folds the entry into the real spec** in `Documents/Sotto`, then re-syncs `docs/` with the `cp` line in `docs/README.md`.
4. **Anthony flips the last column to Yes** and notes the resulting spec version.

Until step 4, the copies in `docs/` are out of date on that point. That is fine as long as everyone knows it — which is what this file is for. **A `No` in the last column means the spec copy is wrong about that thing right now.**

**What belongs here:** anything that contradicts, extends, or resolves the spec. A new tier-2 token row. A constant that turned out wrong in practice. A rule that could not be implemented as written. An open issue from §12 that got settled. A number that was a placeholder and is now real.

**What does not:** ordinary implementation choices the spec does not speak to. Naming a Swift type, splitting a file, picking a collection type, an ordinary layout dimension, a spacing value, a local default. If the spec has no opinion, neither does this file. **An authored value is not automatically a row either** — a trivial one-consumer constant is not a token and not an entry (`rules/design.md` §9).

**The test is cost of reversal, not how unsure you were.** A choice Anthony can overturn in one word belongs in your reply, once, not in this file forever. Every row here is read by every future session; a row that did not need to exist is a permanent tax on all of them.

## Entry template

```
| YYYY-MM-DD | N | <the decision, one sentence> | <why — name what lost and why it lost> | §X.Y or — | No |
```

Columns: **Date · Slice · Decision · Reason · Supersedes · Folded back into spec?**

---

## Decisions

| Date | Slice | Decision | Reason | Supersedes | Folded back? |
|---|---|---|---|---|---|
| 2026-09-03 | 10 | **`.popUpMenu` + `.transient` + `orderFrontRegardless()` is not the whole of the HUD's full-screen fix, and the overlay now takes the other two halves as well: `show()` re-asserts `level` and `collectionBehavior` on every showing rather than only at `make()`, and rebuilds the panel when `windowNumber <= 0` before ordering front. Separately, the docked panel's reveal insurance is armed *before* the one-sample-at-a-time guard instead of after it.** | The row below claimed the overlay was fixed by level and behaviour alone; it was not, and Anthony reports it regressed. `HUDPanel` carries three things, and only one was copied. A panel that has survived a Space change or a wake can come back without its auxiliary status, so a behaviour set once at construction goes stale — that is why `HUDPanel.panel(orderingFront:)` re-states both every time. A window-server reconnect leaves the `NSPanel` alive with an invalid `CGWindowID`, and every `orderFront` on it is a silent no-op — `HUDPanel.ensurePanel`'s guard. The third is the overlay's own: a docked show sets `alphaValue = 0` and waits for the backdrop sample, and the `sampleInFlight` guard returned before scheduling the 1.5 s insurance, so a show that found a capture still running from the *previous* showing had no route back to visible at all — that capture carried the old showing's `reveal: false` and left the panel invisible until the next show. The reveal is now read as state at completion, so whichever sample lands first reveals. | Extends the `.popUpMenu` row below (2026-09-03) | No |
| 2026-09-03 | 10 | **A send from the overlay's bare New Chat bar retargets the draft onto the chat it just created**, so `isDocked` goes true, the surface switches from Frame 3 to Frame 2, and the reply streams into a conversation the user is looking at. The `.new` branch that reset the draft to `Draft()` is gone; one assignment now serves both targets. **Related: a chat's folder is written when its user turn is committed, not when its reply finishes** — `ChatConversation.drain` saves immediately after `sendMessage` returns. | Anthony: sends from the overlay's new-chat bar "just get sent in the background but my actual view doesn't switch to the panel view to see it." Docking is a property of the draft's target, so naming the new slug is the whole switch — no second state. The early save is what the main window needed for the same complaint: `ChatSession` saved only at the end of the stream, so a new chat had no folder for the whole of its first generation, `ChatLibrary.refresh()` found nothing, and the selection the send had just made was cleared as a chat deleted from under it — the pane stayed titled "New Chat" with nothing highlighted in the sidebar. `ChatLibrary.apply` additionally keeps any selection `ChatConversations` still holds open. | — (fills a gap in §5.8 / §10.2) | No |
| 2026-09-03 | 10 | **The main window's title bar is visible again, and the Chat/Audio detail's name, timestamp, and pin live in it — not in an in-content header strip.** `MainWindowController` sets `titleVisibility = .visible` (was `.hidden`); `ChatDetail` and `AudioDetail` drive `.navigationTitle` / `.navigationSubtitle`, and the pin is a `.primaryAction` `ToolbarItem`. The transparent titlebar, `.fullSizeContentView`, and edge-to-edge sidebar are unchanged — Safari and Mail show a title in exactly this shape. The Audio detail's language badges, which `.navigationSubtitle` (text only) cannot carry, become a thin row above the transport. | The 2026-08-15 Safari-shape note hid the title on the assumption the detail would always carry its own header strip; slice 10's two detail panes each grew one (title + date + pin), which is the thing macOS's own title chrome is for. Moving it there removes a bespoke strip per pane and the empty titlebar gap above it, and puts the pin where a document's pin/lock control sits in system apps. | Contradicts the "title hidden" half of the 2026-08-15 Safari-shape note (DECISIONS.md, that date; spec §10.2 as amended) | No |
| 2026-09-03 | 10 | **The main window's composer is one Liquid-Glass bar — `OverlayView.panelComposer`'s treatment (`GlassEffectContainer` + `.glassEffect(Token.Material.overlay, …)` + `specularRim` + the three-stop shadow), reused verbatim, at pane width with `.padding(.horizontal)` / `.padding(.bottom)` so it floats off the edges. The conversation list above it is unchanged — no glass on the transcript.** | Anthony: "there is one glass layer on the chat bar where the user types the message, the actual conversation stays the same." The overlay's docked composer already is that bar; the window's composer was a plain padded `VStack`. Reusing the overlay's modifier stack (no new tokens) makes the two chat bars read identically (§0.3). | — (extends §10.2 / §6.4; no measurement changed — reuses the slice-9 overlay-intrusiveness tier-2 values) | No |
| 2026-09-03 | 10 | **The New Chat screen is the glass composer bar alone, centred, with no copy anywhere on the pane — no title text, no `ContentUnavailableView`, no scroll region. The first send flips the pane to the conversation layout on the same runloop** (`commitAndGenerate` appends the user turn synchronously and sets `ChatLibrary.selection`, so `messages.isEmpty` goes false and the window title populates at once). A pending `ChatConversation` is guaranteed to exist by `hydrate()` calling `beginNew()` when nothing is selected. | Anthony: "The chat bar should be in the center of the new chat screen and look like the other chat bars… No text should appear on the new chat screen… After sending a message in new chat, the window should switch to a chat screen." The old New-Chat state showed a `header` with "New Chat" and, when `active` was nil, a "No Chat Selected" `ContentUnavailableView`. | Supersedes the `ChatDetail` "No Chat Selected" empty state (slice 10 as first built) | No |
| 2026-09-03 | 10 | **The overlay panel is raised to `.popUpMenu` level (was `.statusBar`) with `.transient` added to its collection behaviour and `orderFrontRegardless()` (plus the window-server-returned-nothing retry) after `makeKeyAndOrderFront`, so it appears over full-screen apps.** It still activates and becomes key — the one difference from the HUD, which never takes focus. `NSApp.activate` on show and `previousApp.activate` on hide already return focus to the full-screen app on close. | Anthony: the overlay "used to appear over full-screen apps no problem" — it did not; `OverlayPanel` has been `.statusBar` since the first commit. The proven in-repo fix is the HUD's, from commit `2b6ef70`: `.statusBar` "did not reliably sit above a full-screen app's window ordering" and a plain order-front is clipped to the app's own Space. | — (mirrors the HUD's 2026-08-24 `2b6ef70` fix onto the overlay; no spec text) | No |
| 2026-09-03 | 10 | **The docked overlay panel scrolls to its newest turn every time the overlay opens.** `OverlayPanel.show()` posts `.sottoOverlayDidShow`; `ConversationView` jumps to the bottom on that and on `.onAppear`, clearing `detached` so an arriving stream keeps following. Needed because re-showing onto an already-docked chat does not remount the view, so `onAppear` alone only fires the first time. | Anthony: "When I open the overlay and a chat panel shows up, it should automatically scroll to the bottom." `ConversationView.load()` early-returns for a live conversation, so the docked panel never scrolled on open. | — (fills a gap in §5.8's docked-panel behaviour) | No |
| 2026-09-03 | 10 | **The user's own turns are labelled `YOU`, in the same caption weight and tracking as the reply's `SOTTO` label.** The per-turn model sub-label stays on the reply only. | Anthony: "messages by me should say 'You' in the same way Sotto's messages say 'Sotto'." The turn anatomy (`ConversationTurnView`, shared by both surfaces) labelled only the assistant turn; the chat SVG's "small speaker labels" reads naturally as both sides. | — (extends the chat-turn anatomy in `rules/design.md` §11.2) | No |
| 2026-09-02 | 10 | **§10.4 priority 3 (stop chat generation) already generalizes to a main-window-only generation with the overlay fully closed — no code change needed.** `OverlayPanel.escRemainderFromTap`'s priority-3 branch guards on `NSApp.isActive` and `Activity.shared.active.contains(.generating)` only; the check on the overlay panel's visibility exists solely for priority 4 (close overlay), one branch down. `ChatSession.sendMessage` sets `Activity.shared.set(.generating, true)` regardless of which surface started the send, so Escape already stops a main-window-only generation today. | Slice 10 adds a generation path (the window's chat detail) that the mechanics were not written against, so the question was live rather than assumed; reading the branch settled it without a change. The 2026-09-02 "tap's Escape coordination stops blocking on main" row's one behaviour delta — priorities 3/4 fire whenever Sotto is frontmost and the overlay is up — already implies this, but did not say so for the overlay-closed case, which is the one slice 10 actually needed. | — (clarifies the row below; no mechanics changed) | No |
| 2026-09-02 | 10 | **The Chat sidebar's row context menu gains Delete… behind a confirmation, and Reveal in Finder — mirroring the Audio sidebar's 2026-08-20 decision exactly, same confirmation copy and shape.** | Chats had a pin (§9.1) but no removal — the same gap the Audio sidebar had before its 2026-08-20 decision. One control in two modes (§10.2) means one fix, not a second one reasoned out from scratch. | — (extends the 2026-08-20 Audio sidebar decision to Chat) | No |
| 2026-09-02 | 10 | **The main window's Chat detail carries its own composer at the bottom of the pane — the same field, chips, add menu, and send button the overlay uses (`ComposerField`/`ComposerChips`/`ComposerAddMenu`/`ComposerSendButton`), reused rather than rebuilt.** | §10.2 describes the workspace shape, not a compose surface inside it, and the build order does not say whether the window sends or only reads. A chat mode with no way to send from it is a history viewer, not chat in the main window; reusing the overlay's composer views keeps one implementation of type/attach/send instead of two that would drift. | — (extends §10.2) | No |
| 2026-09-02 | 10 | **Generation wires into both the main window's Chat detail and the overlay's docked panel, not the main window alone — the same `ChatConversation` instance renders in whichever surface has it open, and a reply streamed from one is visible in the other with no reload.** `ChatConversations`/`ChatConversation` (`Sotto/Chat/ChatConversations.swift`) are the one live layer both surfaces read; the overlay's send now goes through it too, retiring the read-modify-write of `chat.md` it did on its own since slice 9. | The build order's slice 10 line names the main window because the overlay's chat predates it, but the overlay already sends and generates (slice 9) — confining the live layer to the window would leave two chat implementations for as long as both existed, one live and one still hand-rewriting the file underneath a session that might be mid-save. "One writer" (`ChatSession`, the actor) only holds if every sender goes through it. | Build order's slice 10 "main window only" generation line; extends the 2026-09-01 "generation stays a later stage" row | No |
| 2026-09-02 | 9 | **The tap's Escape coordination stops blocking on main. The four `DispatchQueue.main.sync` calls in `EventTap.handle` are gone: a per-press mark under an `NSLock` is written on the tap thread before the event is delivered, §10.4's priorities 2–4 move onto main entirely — `OverlayPanel.escRemainderFromTap`, dispatched asynchronously by the tap for every Esc it does not abort — and the panel's `cancelOperation` only consumes the mark and stands down, running nothing itself.** The sync it replaces did 2–3 main-thread round trips per Esc pressed **in any app, Sotto idle or not**, and the tap is a `.defaultTap` at the head of the session — a callback parked on main stalls keyboard delivery system-wide until macOS times the tap out. The mark set before delivery is the ordering the sync used to buy, so exactly-one still holds. **One behaviour delta:** priorities 3/4 now fire whenever Sotto is frontmost and the overlay is up (e.g. HUD keyed), not only when the overlay panel is key. | PR #24 review flagged the sync; its deadlock framing was wrong (nothing on main ever waits on the tap thread), but the stall is real and system-wide, and the idle fast path's `Activity.isIdle` sync-read went with it — `Activity` is a plain `@Observable`, not `@MainActor` as the deleted comment claimed. The cost of going non-blocking is one cheap async no-op per idle Esc where the old code paid two synchronous round trips. Known residual, documented at the mark: two Escs inside a single main-blocked pass can double-fire — a window the sync design closed, but only by buying it with the stall on every press. | §10.4's mechanics (the stack's semantics — one action top-down, Esc never swallowed, dedicated tap thread — are unchanged) | No |
| 2026-09-02 | 9 | **Draft saves coalesce for 300 ms and flush on overlay hide and on quit; the persistence promise becomes "exact across close and normal quit; a force-kill loses at most the last 300 ms of typing."** Until now every keystroke saved synchronously on `draft`'s `didSet`, and an image attachment made each save re-encode and rewrite the whole image — a multi-MB base64 write on the main thread per keystroke while typing. | PR #24 review follow-up; Anthony chose **debounce + flush** over per-keystroke saves and over debouncing image-drafts only. Along the same paths: failed saves/loads/send writes now log through `DraftStore.log` instead of vanishing into `try?` (the send catch was silently discarding real disk failures under a validation-only comment), the dead `isLoading` flag went with the rewrite, and two identical empty-draft delete branches collapsed to one. | §5.2's "exact text+attachments across close AND kill/relaunch", on the force-kill tail only | No |
| 2026-09-02 | 9 | **Correction, same session, to the row below: the capture is ScreenCaptureKit's `SCScreenshotManager`, and the no-flash mechanism is "order front hidden, reveal on first pin" — not a synchronous capture before ordering front.** The decision is unchanged; the implementation detail in the row below was written before the build and is wrong on this point: `CGWindowListCreateImage` is not merely deprecated, it is **obsoleted** in the installed SDK (macOS 26.5 marks it unavailable), so the synchronous one-shot does not exist to call. The async cost is carried by `OverlayPanel`: at show time the docked panel orders front with `alphaValue` 0 — key and typeable immediately — and reveals on its first polarity pin, so the wrong-polarity flash the sync call would have prevented never renders. The filter excludes every Sotto window by bundle (wash and HUD can never sample themselves), is cached across the ~1 Hz refreshes, and self-heals by re-enumerating after a failed capture. | Found by the build, not by reasoning — the compiler rejects the obsoleted symbol. The reveal-on-first-pin trade: the surface appears one capture's width (~tens of ms) later than a bare-bar show; typing focus is unaffected because the panel is key throughout. A second-window consequence of the modern API is recorded in `BackdropSample`'s header: the filter is *excluding*, not *below-window*, so another app's window floating above the panel would be sampled as backdrop — `.statusBar` level makes that rare, and the refresh converges. | The row below, on its named-rejection sentence and its "completes before the panel orders front" claim only | No |
| 2026-09-02 | 9 | **The docked panel's light/dark polarity is keyed to a sample of the backdrop behind it, and Sotto acquires the Screen Recording grant at the first docked open.** At `show()` with a docked target, `BackdropSample` captures everything below the panel's window (`.optionOnScreenBelowWindow`, so Sotto's own windows never sample themselves), averages the region in sRGB, and pins the panel's **window appearance** to the result — which drives the wash's scrim, the conversation's semantic colours, and the specular rim from one seam. Re-sampled at ~1 Hz while the docked panel is visible; the pin is lifted on hide and on retarget to the bare state, which is never pinned. | Anthony's ruling, made after the mechanism was laid out: the ask-bar inside the panel was already glass and already flipping correctly — it samples the wash's own darkened field — so the one thing keyed wrong was the wash, and fixing its polarity makes the whole panel coherent from one change. Grant timing follows §5.6's deferred-to-first-use pattern (the screenshot gesture holds the same grant); an ungranted machine gets nil samples and falls back to the system appearance, which is the behaviour this replaces. **Averaging in sRGB is deliberate** — the window server blurs and the glass flips in gamma-encoded sRGB (`rules/design.md` §4.3), so the same encoding is what lets the sample agree with what the glass would decide over the same backdrop. **Named rejection: `SCScreenshotManager`** — async-only and needs `SCShareableContent` enumeration plus a per-display filter, while the synchronous `CGWindowListCreateImage` completes *before* the panel orders front, which is what prevents a wrong-polarity first frame; the seam is one function if it must move. Threshold 0.5 is a local constant (single consumer, one edit — `rules/design.md` §9, not a token). **The bare bar is untouched**: still unpinned glass, still flipping per frame. | Today's "the docked panel's light/dark polarity stays keyed to the system appearance" row — the Screen Recording ruling that row reserved for Anthony has now been given, and the platform-limit framing it recorded no longer holds | No |
| 2026-09-02 | 9 | **The docked panel's light/dark polarity stays keyed to the system appearance. It cannot be keyed to backdrop luminance the way the bare bar's glass is — that is a limitation, not a choice, and it needs Anthony's ruling on whether to buy the fix with a Screen Recording grant.** Anthony asked for the panel to "activate the same way the light mode for the chat bar on new chats work," having seen the panel render dark grey over a white backdrop while in dark mode. The bar's behaviour is not something the app does: Liquid Glass flips its content's polarity in the render server. **Measured 2026-09-02: that flip is invisible to every in-process API.** System pinned Dark over a white backdrop, text inside a `.glassEffect()` rendered *black on light glass* while `@Environment(\.colorScheme)` said `dark`, `NSView.effectiveAppearance` said `NSAppearanceNameDarkAqua`, and no `onChange(of: colorScheme)` fired; over black, the same view rendered white on dark with identical readings. An earlier run appearing to show `effectiveAppearance` tracking the backdrop did not reproduce (1 of 3 white runs, and 1 of 1 black runs, returned `Aqua` — noise). So the app cannot read the glass's conclusion. | The two ways to give the panel the bar's behaviour both fail here. **(1) Make the wash glass.** Ruled out on four counts, three already measured: glass lifts pure black to 27/33 against the effect view's 15, dissolves text at least as completely (SD 2.2/0.6), and — the structural one — `glassEffect(in:)` takes only a `Shape`, so it cannot express the 40 pt feathered edge that is the whole point of the field, and neither `alphaValue` nor an ancestor mask can add one without killing the effect. **(2) Sample the backdrop.** Works, and needs pixels: `SCScreenshotManager` and `CGWindowListCreateImage` both require the Screen Recording grant. That is a consent-rule product decision under `CLAUDE.md` §2 — Sotto currently defers the grant to §5.6's screenshot gesture, and this would move it to overlay-open time. **Not taken here.** A permission-free approximation was considered and rejected as worse than nothing: `CGWindowListCopyWindowInfo` gives window bounds without the grant, so "is anything covering the panel?" is answerable, and `NSWorkspace.desktopImageURL` gives the wallpaper — but the case Anthony reported *was* a white app window, which is exactly the case that heuristic cannot see. | — (extends the 2026-08-19 §6.7 glass-flip measurement with what it does *not* let the app do) | No |
| 2026-09-02 | 9 | **The wash's scrim was very nearly inert and is now a sibling view; the ruled scrim alphas 0.30 dark / 0.42 light stand unchanged; the docked content's side insets become equal at 34 pt.** (1) The scrim was added with `layer?.addSublayer(_:)`, which put it *below* the effect view's own backing layer — AppKit owns the z-order of subview layers and a hand-added sublayer does not reliably sit above them. Forcing its alpha to 1.0 moved the field only to 152 where an over-composited white would give 255; as an `NSView` added with `addSubview(_:positioned: .above, relativeTo: blur)` the same probe reads 245.5. (2) The insets were 24 leading / 44 trailing, which left the content 10 pt left of the wash's centre — Anthony: "the chat bar when in the chat panel seems to not be centered compared to the background… needs to be moved right a little bit." | (1) Found by probe, not by tuning: raising `lightScrimAlpha` from 0.42 to 0.58 moved the render only 135 → 139.8, which is not what a 38 % alpha increase does to a compositing layer, so the next step was a decisive alpha-1.0 test rather than a fourth value. **The consequence matters more than the bug**: Anthony's ruled 0.30 and 0.42 had never actually been applied, so the "light mode needs a much stronger scrim" reading was an artefact. With the scrim compositing, 0.42 gives 182 over a dark backdrop and 237 over a light one — dark text clears AA either way — so both ruled values were restored rather than re-tuned. (2) The content had held the position it had in the old 500 pt field while the field grew 20 pt rightward to reach the screen edge. The drawn 24 pt gutter is what gives way, because the drawing measured it against the narrower field; 34 is derived (`(columnWidth + 20 - 452) / 2`), not picked. | **Yesterday's blur-architecture row, on "the content's insets are unchanged"** — that is now false; and its scrim measurements, which were taken through the inert path | No |
| 2026-09-01 | 9 | **The wash's blur is a mix of blurred and untouched backdrop, not a weaker material; the field gains 72 pt squircle corners; the effect view is shaped by `maskImage` rather than an ancestor `layer.mask`.** (1) `blurMix` 0.88 — 88 % of the field is `.hudWindow` behind-window blur, 12 % is backdrop left alone, applied as the alpha ceiling of the same mask that draws the edge feather. (2) The mask becomes a rounded-rect signed-distance field at radius 72, exponent 4 (a squircle, not the circular arc `rules/design.md` §4.1 forbids), which also makes the feather uniform *around the corners* — the old separable `fx(x)·fy(y)` product pinched there. (3) `NSVisualEffectView.maskImage` replaces the ancestor `layer.mask` + `alphaValue 0.5`. **The previous build applied no blur at all**: both of those force offscreen rendering, which silently switches behind-window vibrancy off, so the file documented an effect it did not have. Scrim alphas, feather, and column width are unchanged. | Measured, not tuned. Anthony ruled out further value changes — "try completely different architectures… maybe blur isn't the right option" — so four were built and measured against controlled backdrops. **The radius cannot be reduced**: all ten AppKit materials blur identically and differ only in tint and transmission (0.19–0.58), and there is no public radius control. At full strength every one takes body text to a flat field (local contrast SD 1.1 against the backdrop's 60) — that *is* the "erased it into mush" complaint, quantified. **Glass lost on numbers**, not on its rim: `NSGlassEffectView` lifts pure black to 27 (`.clear`) / 33 (`.regular`) against the effect view's 15 and dissolves text just as completely (SD 2.2 / 0.6). **Mixing sharp backdrop back in was the only architecture that moved the metric**, and it decouples how much structure survives from how dark the field is, which the pure-blur form cannot do at all: swept over body text, 0.60 stays readable, 1.00 is the mush, 0.85–0.92 is the band where glyphs survive as shapes without resolving. In the built panel it takes local contrast from 82 to 17 — "I can tell there is text there, just not what text." **On the gray**: the window server does blur in gamma-encoded sRGB, confirmed (over a 50 % black/white backdrop the field lands at 93, matching sRGB's predicted 94.5, not linear's ~130), so the sRGB-vs-linear diagnosis is correct — but Sotto cannot apply the correction, because `CALayer.backgroundFilters` composites against the app's own layer tree and is a no-op against the window server's pass. Retaining unblurred backdrop restores the brightness the sRGB average removed by a different route. A literal linear-space blur needs ScreenCaptureKit and therefore the Screen Recording grant, which is a consent-rule product change and Anthony's to make — **not taken here.** | Today's "the wash is a black scrim, not a blurred material" row, on the blur's presence and how it is dialled; the same row's square-cornered plateau | No |
| 2026-09-01 | 9 | **The wash is a black scrim, not a blurred material; the field reaches the screen's right edge and the window's bottom edge; the composer field's line box is held at the drawn 24 pt.** (1) The `.hudWindow` behind-window blur is gone — what remains under the plateau mask is one tinted scrim, dark a plain black at 30 %, light the unchanged 42 % white — the blur averaged whatever was behind it into gray and erased it into mush: *"the actual background isn't turning darker as in black. It's like grayed out instead… instead of turning gray, it turns black and there's also less black and also less blurry so I can see a little bit of what's behind without making it difficult to read the text."* (2) The field's right edge moves from 20 pt in from the screen to the screen edge itself, and its bottom from 15 pt above the window bottom to the window bottom — the old rect cut the 20 pt feather before it finished, so *"I can tell exactly where the gradient starts"*; the content's own insets are unchanged, only the wash/world boundary moved. (3) The composer field's paragraph style gains `lineHeightMultiple = 24 / natural` (TextKit 2 ignores `maximumLineHeight`, so the min/max pair never held the row; the natural `.title3` box is ~26⅓ pt) — before this, typed text sat against a caret taller than its own line box and drew off-centre in the bar. | Anthony's same-session report of the three symptoms; each fix measured before it shipped (gray source traced to the blur, seam to the truncated feather, off-centre to the unclamped line box). | The 2026-09-01 uniform-interior row's material basis and edge rects; today's earlier "single strong material" row, on blur and tint | No |
| 2026-09-01 | 9 | **The wash's treatment is uniform across the panel interior; the fade to nothing is confined to one narrow band at the field's edge, the same width on all four sides** (~20 pt, the field's own edge margin). | Anthony, after seeing the plateau render: *"I don't like the look of the increasingly to the middle"* — the 90–118 pt outside-in accumulation read as a visible edge and a gradient peaking where the text sits. His rule: *"the padding that's from the bottom of the screen to the chat panel… the same on all 4 sides to the gradient… 20 point padding all around the panel of gradient until the background, which is all uniform."* The mask formula is unchanged — only the feather shrinks, so the interior sits at full strength everywhere the text can be. | Today's panel row's "blur accumulates continuously from the outside in", and the PDF's six-pass schedule it approximated | No |
| 2026-09-01 | 9 | **The docked composer is the bare bar's own inline→stacked composer at panel width — not a separate two-row control.** First text types on the single row (`+`, field, chat capsule, send); only when the line fills does the draft move to the top row, leaving attach, chat, and send on the bottom row. The field's drawn two-row shape and the capsule-beside-the-text placement are both given up. | Anthony, after seeing the first panel build: *"the chat selectors in the top right should be the same chat bar as if it's a normal chat… upon 1st typing… you stay in the 1st row and type there until you reach the new chat, then you move all the text to the top row."* The separate implementation was also the giant-slab bug — nothing framed the text view, so it grew unbounded. | Today's panel row above, on composer shape and capsule placement; the PDF's always-two-row composer | No |
| 2026-09-01 | 9 | **The send circle takes the ChatGPT-bar contrast treatment, still colourless.** The circle is `primary` at 85 % in light mode (near-black) and 25 % in dark (a lighter wash than the bar, per the reference screenshot); the glyph inverts — text-background colour in light, near-full primary in dark — and an empty draft dims the whole button to 40 %. | Anthony's reference screenshot of ChatGPT's composer, delivering the step the earlier row anticipated: *"the outer circle be lighter which makes it pop without adding color."* The pop is contrast polarity, not ink volume — which is why 12 %/80 % still read as too subtle. §14.3's objection was always a *saturated* button; no hue is involved. | Today's earlier 12 %/80 % row, and through it the 2026-08-26 gap-3 values; the no-colour half of gap 3 stands | No |
| 2026-09-01 | 9 | **The docked chat panel is rebuilt per `Chat panel light and dark mode.pdf` (2026-09-01), and the wash becomes one continuous gradient, not the drawing's six passes.** A 500 pt wash field bottom-right — right edge 20 pt in from the screen edge, bottom edge 15 pt above the window bottom, top edge 24 pt above the first line of conversation, hugging content, ceiling 70 % — with the conversation rendered inside it (read-only from `chat.md` until generation lands) and the composer moved into the field: placeholder "Ask a follow-up", `+` inside at bottom-left, send at bottom-right, the chat capsule kept beside the text (Anthony's placement — the drawing omits the switcher). **The wash is a single strong material masked by a generated plateau** — alpha(x, y) = fx(x)·fy(y), smoothstep to full at ~110 pt inset on all four sides — plus one tint layer under the same mask, light 42 % white, dark 34 % #0A0A0C. | Anthony's PDF carries the geometry, the pass schedule (2/5/10/18/30/46 pt, 90–118 pt soft edges), and the tint peaks; his instruction the same day rules out layered passes: "I want you to do a gradient, not passes of like layers on top of each other." Four answers settle the rest: panel + history (generation stays a later stage); send ink raised without colour; capsule kept; and the mask-feather approximation accepted over a true 2→46 pt radius ramp, which would need Screen Recording — a consent-rule product change Sotto has so far deferred. The radius itself cannot vary spatially in a behind-window blur; the ramp is the mask feather, judged on screen like wash candidate (a) was. | Decision 40's docked shape (560 pt column, 390 × 167 composer) and the 2026-08-27 six-answers wash (a) horizontal gradient — the 500 pt field, in-panel composer, and four-sided plateau replace both | No |
| 2026-09-01 | 9 | **The send button's ink goes up, its colourlessness stays.** 7 % → **12 %** background, 60 % → **80 %** glyph. | Anthony, 2026-09-01: *"keep the ruling but change the percentage, the circle is too subtle so make it a bit more noticable but dont add color yet."* The named next step is a ChatGPT-style lighter outer circle, which pops without colour — that iteration is his to call after seeing this one. | The 2026-08-26 gap-3 row's 7 %/60 % values; §14.3's rejection stands, aimed at saturation, not ink | No |
| 2026-09-01 | 9 | **The overlay ships with a drop shadow after all — three stacked SwiftUI shadows (y4 r6 @ 10 %, y10 r16 @ 8 %, y18 r32 @ 5 %), with `bottomSlack` 36 so the fade doesn't clip at the canvas edge.** Landed by the finish-slice-9-overlay worktree, cited there to Anthony's answer "(B)"; recorded here at merge verification because the row below says the opposite and no entry existed. The window shadow is still `hasShadow = false` — this is the CALayer-side revisit the 2026-08-26 row named, taken through SwiftUI rather than Core Animation. | The code comment's "(B)" attributes it to Anthony's finish-branch ruling; ratified in practice by his testing and retest of builds carrying it without objection. Logged so the record matches the screen. | **The 2026-08-26 no-drop-shadow ruling** on outcome; `Designs for Slice9.pdf`'s "shadow 0 10 28" on values | No |
| 2026-08-27 | 9 | **Six answers on Frame 2 and the overlay, all Anthony's, 2026-08-27.** (1) **The wash is one `NSVisualEffectView` masked by a horizontal gradient** — opacity ramps, radius does not; the stacked-material approximation is not attempted until that has been seen on screen. (2) **The column runs to 70 % of usable height, then the conversation scrolls** — §5.8's proportional half survives the loss of the panel, its 720 pt companion does not, because there is no panel to bound. (3) **The bare bar grows to 144 pt exactly like the docked composer, then scrolls internally** — no earlier cap. (4) **Radius 20 in both states**, not the drawn 18 for the docked composer. (5) **Dock clearance stays 16 pt.** (6) **Right Option only** keeps the overlay; Left Option is not a trigger. | (1) is the cheapest of the three candidates in today's FINDING row and the only one that can be judged without building the expensive one first. (2) keeps a rule rather than a constant, which is the same shape as gap 2's answer. (3) **closes open issue 5** — Frame 3's "same composer" was the drawing already saying so; the worry that a bare bar growing with nothing above it reads as broken did not survive Anthony seeing it drawn. (4) one radius family; 20 is well under half of the 167 pt composer, so the superellipse still has straight edge to blend into. (5) the reference machine's Dock is auto-hidden, so the two-stacked-slabs objection to 16 pt does not arise in practice. (6) restates the 2026-08-15 ruling against a session's worth of drift. | **Open issue 5** in `rules/open-questions.md`, closed; `Designs for Slice9.pdf`'s r18 for the docked composer; §5.8's 720 pt cap for the docked state | No |
| 2026-08-27 | 3, 4 | **Open issue 3 is closed: a focus change mid-transcription routes the text to the clipboard, not to the original target.** | Anthony, 2026-08-27. The original target risks writing into a window the user has left, which is the one failure §4.5 rules out — text arriving somewhere nobody is looking, with no error anywhere. The clipboard is surprising when the field is still right there, and that is the cost, but it is a *visible* cost: the existing no-focused-field path already morphs the HUD to "Copied to clipboard" and fades, so the surface that says what happened is built and needs no new vocabulary (`rules/audio-and-transcription.md` §3). | **Open issue 3** in `rules/open-questions.md` and spec §12's list | No |
| 2026-08-27 | 9, 10 | **Gap 1 is closed. The overlay in an existing chat is a right-docked 560 pt column with no panel edge — `Designs for Slice9.pdf` Frame 2.** Not spec §5.8's bounded floating panel, not decision 04's full-height edge wash as the token sheet recorded it, and not the chat-response SVG's local fade. The column is a **graduated wash over the wallpaper**: six blur passes at 1/2/4/8/16/30 pt with a white wash ramping 0 → 93 % across the same horizontal span, no border, no corner, no surface. **The only drawn object in it is the composer** (390 × 167 pt). Turn anatomy is the SVG's, verbatim: `YOU` / `SOTTO` labels, the quoted source line on a 2 pt accent rule, no assistant bubble, semibold answer over lighter reasoning. | Anthony, 2026-08-27, pointing at the PDF. **The three-way was never three shapes — it was one shape at two zoom levels plus an anatomy.** §5.8's bounded panel survives as **Frame 3**, the centred bar with no chat above it; decision 04's wash is what Frame 2 draws; the SVG's fade was the wash drawn without its measurements. `rules/design.md` §11.2 predicted exactly this — that the SVG's anatomy is separable from the container question and survives whichever container wins. | **Gap 1** in `rules/open-questions.md`, in full; §5.8's "the panel is bounded" for the docked state, which has no panel to bound | No |
| 2026-08-27 | 9 | **Six lines of composer is 144 pt, not 180.** Field type is 15/24 pt, so one line is 24 pt: the field starts at 72 pt (3 lines) in the docked composer and 1 line in the bare bar, grows to 144 pt, then scrolls internally. Composer chrome is pad 13/11/15/13, chips row 27 pt, control row 26 pt. | `Designs for Slice9.pdf` Frame 2, measured. **§5.8's 180 assumed a 30 pt line**, which is neither the drawn type nor what `.body` resolves to. Six lines is the rule Anthony wrote and it is unchanged; 180 was arithmetic on a line height that no longer exists. | §5.8's "grows to six lines (180 pt)", and the same number wherever the build order repeats it | No |
| 2026-08-27 | 9 | **FINDING, not a decision — Frame 2's wash has no macOS API, and it is the one thing in this slice that cannot be built from Liquid Glass.** A six-pass graduated blur of the desktop behind a window, with no edge and no shape, is not `.glassEffect()`: glass takes a shape in `in:` and draws a specular edge and rim refraction on it, which is precisely what Frame 2 says it does not have. | The two primitives that blur an arbitrary backdrop are `NSVisualEffectView` — **uniform radius, and `rules/design.md` §3 bans it as fake glass** — and `NSGlassEffectView`, which is shaped and edged. Neither ramps a radius across a span; nothing in SwiftUI or AppKit does. **Three candidates, none free.** (a) One `NSVisualEffectView` masked by a horizontal gradient: the *opacity* ramps, the radius does not, so the near edge reads as a fade rather than a deepening. (b) Several masked effect views stacked at increasing materials to approximate the radius ramp — closest to the drawing, most expensive, and a stack of materials is the decoration failure §3 names. (c) Drop the ramp: uniform blur plus the white wash gradient, which keeps the edgelessness and loses the depth. **This needs Anthony, before stage 5 and before any of it is written** — it is the material question `rules/slices.md` says to judge only in Swift, and the mockup's flat blur cannot answer it. | Nothing — new. It is the concrete instance of `Design.pdf` page 5's standing warning about mockups approximating material | No |
| 2026-08-27 | 3, 9 | **The two specular streaks now cross the top and bottom centres instead of stopping at them, and the rim flips polarity with the appearance.** Each highlight becomes a **105° plateau** — 20°→125° and 200°→305° — so the bottom streak runs past 6 o'clock and dies in the bottom-left, the top streak past 12 o'clock and dies toward the top-right, and the quiet stretches move from the corners to the **left and right midpoints**. The fixed `Color.white` highlight and `Color.black` shadow lobes are replaced by **one polarity, `Color.primary` at two strengths** — 0.28 lit, 0.05 resting — so the rim is dark over a light appearance and light over a dark one. `Specular.shadow` is deleted. | Anthony, 2026-08-27: *"both streaks, on the top and bottom, end when they hit the middle… I want the streak that ends on the top middle go in between the right and middle on the top and the streak that ends on the bottom to instead end bottom left,"* and *"make the streaks dark on a light background so its flipped."* The 50° plateaus of the day before ended at 70° and 250°, twenty degrees short of the edge centres, which is exactly the stopping-at-the-middle he is describing; crossing the centre is what makes the two marks read as one edge lit from above-left. **The polarity change is the answer to the flag left on yesterday's row** — a white streak at 0.28 over light glass was close to invisible, and the black lobes did nothing over dark glass, so both were fighting the surface rather than lying on it. `.primary` is a semantic colour, so this is the tier-1 answer to a tier-2 problem and the row gets cheaper, not dearer. **The 180° period is unchanged.** | The 2026-08-27 specular row, on the stop table and the colours only; `width` 0.75 and the tier-2 claim stand | No |
| 2026-08-27 | 9 | **The overlay activates Sotto when it opens, and its panel is no longer `.nonactivatingPanel`.** `show()` calls `NSApp.activate(ignoringOtherApps: true)`; `hide()` hands focus back to the app that was frontmost with `activate(from: .current)`. The activation **policy** is untouched — Sotto stays `.accessory` and `setActivationPolicy` is still never called. | Anthony, 2026-08-27: *"sometimes when I open the overlay there is a caret left behind on the screen, leaving duplicate carets… I want the text indicator cursor only shown on the bar."* **The flag was the cause, not a symptom.** `.nonactivatingPanel` asks the window server for key input *without* deactivating anyone, so the app behind never resigns first responder and goes on blinking an insertion point that no longer receives keystrokes — two carets, one of them lying. Deactivating the other app is the only thing that stops it drawing, and Spotlight does exactly this. **The cost stage 1 was protecting against does not arise:** an `.accessory` app has no menu bar, so activating it takes nothing off the screen; the app behind keeps its windows and gets focus back on Escape. **Two measured API notes.** A plain `NSApp.activate()` is *denied* — macOS 14 cooperative activation refused it from a background process and the frontmost app never changed — so the deprecated `ignoringOtherApps:` parameter is load-bearing and has no replacement. On the way back, a bare `previousApp.activate()` dropped focus to the **Finder**; `activate(from: .current)` is the handoff that works. | Stage 1's brief for this slice, which specified `.nonactivatingPanel`, and the reasoning in `OverlayPanel`'s own header comment | No |
| 2026-08-27 | 3, 9 | **The specular rim gets longer, dimmer, and thinner, and it is now drawn in one place instead of two.** `highlight` 0.5 → **0.28**, `shadow` 0.12 → **0.07**, `mid` 0.08 → **0.04**, `width` 1 → **0.75**, and each highlight becomes a **50° plateau** (20°–70° and 200°–250°) rather than a single angle ramping 90° to the next stop. The drawing moves to one `View.specularRim(radius:)` in `Token.swift`; `HUDView` and `OverlayView` both call it. | Anthony, 2026-08-27, against an AirDrop-tile reference: *"make the streak highlights a little longer but more subtle globally… less bright and/or thinner."* **The original row said its own strength was provisional and that `highlight` at 0.9 read closer to chrome than to Apple's rim** — it had already come down to 0.5 and was still too present. Length and subtlety are one change, not two: the old stops put the peak at a point, so the only way to see the streak was to make the peak bright, and a bright point is what reads as chrome. Holding it across 50° and returning to `mid` before each dark lobe buys the length back at a third of the brightness. **The 180° period is unchanged** — one light source above-left, caught twice on opposite edges. **The de-duplication is the second half of the instruction** and `CLAUDE.md` §0.3: the *values* were already shared through `Specular`, the four lines that stroke them were copied into both views. **This changes the HUD's appearance, which was not the point of the overlay work and is reported under §0.4** — deliberately, because Anthony asked for it globally. | The `specular.width/highlight/shadow/rim` row of 2026-08-19, on values only; the row's structure and its tier-2 claim stand | No |
| 2026-08-27 | 9 | **The overlay bar's corner radius is 20, not the drawn 16.** | Anthony, 2026-08-27: *"increase the corner radius a bit more."* **The ceiling is 26 and it is a hard one, not a preference:** `barHeight` is 52, and at `r = h/2` a `.continuous` corner and a circular arc are the same curve because no straight edge is left for the superellipse to act on (`DECISIONS.md`, 2026-08-18). 20 keeps 6 pt of flat edge on the short side, so the corner is still a real squircle; anything from 24 up starts trading the superellipse for roundness without saying so. Every corner in Sotto goes through `Token.shape`, which passes `style: .continuous` — no call site constructs its own `RoundedRectangle`. | `Designs for Slice9.pdf`'s "radius 16 pt", `sotto-tokens.md` §6.1, and the build order's slice 9 line repeating it | No |
| 2026-08-27 | 3, 9 | **FINDING, not a decision — a Sotto process that has been running across an overnight sleep stops putting its panels on screen, and the gesture path goes on working underneath.** Both `HUDPanel` and `OverlayPanel` are affected together. Quit and relaunch restores both. | **Reproduced on Anthony's machine 2026-08-27**, on a process launched 22:40 the previous evening and left running through five `Maintenance Sleep` cycles (`powerd`, 07:39–07:53). Symptoms as reported: a double-tap of Right Option flipped the menu-bar icon to awake with no bar on screen, and a dictation transcribed with no HUD. **The event tap was healthy throughout** — `OVERLAY` at 08:29:44–49 and `ARM`/`PUSH_TO_TALK`/`STOP` at 08:31 are all in the log — and so was the main thread, which answered Accessibility queries. What was gone was the windows: `count of windows` returned 0 immediately after a signal that had just called `makeKeyAndOrderFront`, with no exception, no fault, and nothing in the process's log. On the same build, relaunched, one double-tap put the bar on screen and one held gesture put the waveform on screen, both first try. **Not introduced by slice 9** — `HUDPanel` is slice 3 and untouched — and the shared surface is the pattern, not the code: a `.borderless` `.nonactivatingPanel` at `.statusBar` level, created once and kept for the process lifetime. The two candidates are the window-server connection not surviving the sleep cycles, and the retained panel object outliving something it holds; **neither is tested**, and a repro costs a night. **The cheap mitigation, not yet built:** rebuild the panel rather than reusing it when a show finds it non-visible, or drop both panels on `NSWorkspace.didWakeNotification`. | Nothing — new. It is why `rules/input-and-insertion.md` §5.1's "suspect the harness first" needs a fifth entry: **suspect the process's age** | No |
| 2026-08-26 | 9 | **Gap 2's overlay half resolves as a rule, not a value: the bar's bottom edge sits 16 pt above the bottom of `NSScreen.visibleFrame`.** `OverlayPanel.dockClearance`. Not a constant distance from the screen bottom, and not the token sheet's 118 pt. | This is the decomposition `rules/open-questions.md` predicted — "clears the Dock's current height by N pt" — with the Dock's height **read from the system rather than assumed at 84 pt**, which is what makes it a rule instead of a second constant. `visibleFrame` also answers the two cases the decomposition explicitly did not cover, with no branch of Sotto's own: a Dock on the **left or right** subtracts nothing from the bottom, so the bar drops to the same 16 pt above the screen edge; an **auto-hidden** Dock leaves only its reveal strip, so the bar sits low and the Dock slides up *under* it — the panel is at `.statusBar` level, above the Dock's, so the bar stays readable when it does. Measured on the reference display (1637 × 1024): Dock shown, `visibleFrame.minY` = 75, bar bottom 91 pt above the screen bottom, against `Designs for Slice9.pdf`'s 88 pt on its 1440 × 900 desktop — the rule reproduces the drawing without being told the number. **The clearance itself is the part still open:** 16 pt is the design PDF's ("16 pt clear of the dock"); the token sheet's 118 = 84 + **34** is more than twice that, and over a real Dock at 16 pt the bar and the Dock read as two stacked slabs with very little air. Screenshot supplied; one edit to change. | **§6.1's** "bottom edge 118 pt above the screen bottom" in `sotto-tokens.md`, the same number in the build order's slice 9, and the **overlay half of gap 2** in `rules/open-questions.md` | No |
| 2026-08-26 | 9 | **Gap 3 resolves in favour of both documents: the send button is present, and it is not saturated.** A 26 pt circle filled at 7 % ink carrying a 60 % ink glyph — third in weight after the caret and the chat control, and quieter than the placeholder text beside it. `Design.pdf` decisions 01 and 04 drew it as a **filled accent circle**; that treatment is what §14.3 warned against and it does not ship. | §14.3 rejects "a saturated send button — the loudest thing in Gemini's bar"; §5.8 restores the button, "not its volume." Read together they are one instruction, not a contradiction: the objection was to **volume**, and the button can be present at a volume that does not trigger it. `Designs for Slice9.pdf` draws exactly that and is the newer of the two drawings. What lost: the accent-filled circle, and (from v0.15) cutting the button entirely, which the design process had already reversed on composition grounds — with a leading `+` and nothing trailing, the bar has no close. **The document that needs amending is `Design.pdf` §6.1/§6.4's accent circle, not §14.3.** | **Gap 3** in `rules/open-questions.md`; `Design.pdf` decisions 01 and 04 | No |
| 2026-08-26 | 9 | **`material.overlay` is a tier-1 token resolving to `Glass.regular`** — the second of the two materials `Token.Material` reserves by name. Four claims: role `material.overlay`; first consumer `OverlayView`, slice 9; tier 1, so no authored value to rule out; added slice 9. | **`.regular` where the HUD is `.clear`, and the difference is what each surface carries.** The HUD holds a waveform and one short string, so `.clear`'s harder refraction is free and Anthony ruled for it on that basis (2026-08-19). The overlay is 600 pt of editable text the user is reading *while they type it*, over an arbitrary wallpaper — `.clear` would refract the backdrop through the one thing that has to stay legible. Slice 0's table names Spotlight as this surface's reference and Control Center as the HUD's, and Spotlight is the denser material. Not `.interactive()`: that is the press-and-flex response for a glass control, and the bar is a container of controls rather than one. **Expect Anthony to rule on this from the renders as he did the HUD's twin.** | Nothing — new row. `sotto-tokens.md` §1's empty state | No |
| 2026-08-26 | 9 | **The overlay ships with no drop shadow. `Designs for Slice9.pdf`'s "shadow 0 10 28" is not ported.** `panel.hasShadow = false`, as `HUDPanel` already does. | Two reasons, one of them mechanical. **Mechanically**, the panel is a transparent canvas larger than the bar so later stages can grow the surface without resizing the window, so an `NSWindow` shadow would fall from the canvas rather than from the visible surface. **By rule**, glass already carries its own shading and a specular rim, and §14.3 asks for a justification past two surface treatments — material plus rim is two. The mockup's flat blur draws no adaptive tint and no specular edge, so a shadow is doing work there that the real material does itself. **If the bar turns out not to separate from a busy backdrop, this is the first thing to revisit** and it would need a `CALayer` shadow on the glass rather than a window shadow. | `Designs for Slice9.pdf`'s base-panel caption | No |
| 2026-08-25 | 8 | **The Models pane's search field is a local filter over the pane's own rows — downloaded, built-in, and the shipped catalog. It never queries the network.** | Anthony, 2026-08-25, ruling on the design PDF's "Search models" field against §7.4's "a curated list, not a search field." What §7.4 rejected — a catalogue browser that phones home — stays rejected; filtering rows Sotto already holds needs no connection and no consent waiver, so the field is admissible without amending principle 1. What lost: no field at all, which the pane's first pass had chosen. | Extends §7.4 | No |
| 2026-08-25 | 8 | **Lab badges take a derived monogram: the badge colour is computed from the lab's name, not authored per lab.** Four claims: role `badge.labTint` (derived, nothing stored per lab); first consumer `ModelsPane`, slice 8; rejected system value — none exists, Contacts' monogram-colour derivation is private and `NSColor` has no name-to-colour API; added slice 8. The hash lands in the system named hues, so no hex is authored, and the hash is hand-rolled because `String.hashValue` is not stable across launches. | Anthony chose the derived monogram over six per-lab hex values, which would have been six tier-2 rows of permanent debt (`rules/design.md` §9). One algorithm over a system palette is the smaller claim. | Extends §14.2's token sheet | No |
| 2026-08-25 | 8 | **The Add row offers a Hugging Face repo id only; the design PDF's "or a local GGUF file" option is dropped.** | GGUF implies a llama.cpp-class runtime §2.2 does not have — Sotto's rungs are on-disk MLX weights, HF MLX repos, and delegated `ollama pull`. Anthony: "for now stick to keeping it simple." What lost: the render's wording, which promised a runtime that does not exist. The spec itself never offered GGUF, so this corrects the design PDF, not the spec. | Design PDF (2026-08-25), against §2.2/§7.4's source ladder | No |
| 2026-08-24 | 3 | **`Transcription.finish()` bounds its wait on the analyzer's result sequences.** Two seconds after `finalizeAndFinishThroughEndOfInput()` returns, if the collectors draining `SpeechTranscriber.results` and `SpeechDetector.results` have still not finished, Sotto cancels them and takes whatever they accumulated. `Transcription.resultGrace`. | **This is the stuck-HUD bug, and the cause is Apple's, not Sotto's.** Reproduced 2026-08-24, **3 of 4 rounds**, by eight ~150 ms dictations 40 ms apart: finalize returns in 0 ms and the transcriber's `results` sequence then never finishes, so `collector.value` suspends forever. `sample` shows no frames — it is a suspended continuation, not a blocked thread. That one suspension is the entire reported symptom: it holds `Dictation.pipeline` non-nil, and `pipeline == nil` is the guard on both `arm()` and `start()`, so every later gesture is a silent no-op; `finish(with:)` never runs, so the HUD is never hidden and `Activity.recording` is never cleared. Confirmed by probe while wedged: `pipeline=true idle=false act=recording hud=true` — **the menu-bar icon is stuck awake too.** **Why a bounded wait is the cause's fix and not a watchdog:** by the time it starts, Sotto has finished the input stream and finalize has *returned* — everything Sotto owes has been paid, and the only missing signal is Apple's end-of-sequence, which cannot be detected by waiting longer. It resets no Sotto state, sets no flag and hides nothing; it ends the wait, and the ordinary path completes and clears the state itself — measured, the draft returns **1 ms** after the grace fires. Two seconds against a healthy 0–1 ms is three orders of magnitude of headroom, so it cannot expire on a working recording. **`analyzer.cancelAndFinishNow()` was tried first as the escalation and does not work** — the grace fired, the call was made, and 4 of 5 rounds still wedged; it is still called, after the cancels, because leaving an analyzer unfinished is worse than calling it. What else lost: cancelling the collectors as soon as finalize returns, which the API name suggests is safe and would risk dropping a last result on every dictation to fix a rare one; and a watchdog over `Dictation`, which would hide the symptom and leave the leak. **Isolated short dictations do not trigger it (0 of 5), nor do two 500 ms apart (0 of 5)** — it takes recordings back to back. After the fix: **0 of 6** on the same burst, and 0 of 6 again on the stripped build. | Extends §4.7 | No |
| 2026-08-24 | 3 | **The microphone opens on the Right Cmd key-down, before the gesture is classified, and the audio is thrown away if the press turns out not to be a dictation.** `Dictation.arm()` starts capture and puts the HUD on screen at zero alpha; a release under the 250 ms threshold, a chord, an Escape, or the second-tap window expiring all route to the **existing** `abort()`, which discards the buffer and inserts nothing. Guarded on `AVCaptureDevice.authorizationStatus` so an unauthorised first run arms nothing and the real gesture still asks at first use (§2.4). | Anthony, 2026-08-24. Measured: `engine.start()` costs ~54 ms warm and ~162 ms cold, and that is CoreAudio bringing the hardware up — installing the tap early and pre-preparing the graph removed everything around it but not that. The only remaining lever was to stop paying it after the user has committed. Warm, mic live **336 → 54 ms** and first audio buffer **441 → 156 ms**; the recording now carries **~197 ms of lead-in** the user spoke over the threshold and previously lost. **The cost, stated plainly:** a press that never becomes a dictation opens the microphone for **50–85 ms** typically and at most **~240 ms** — bounded by the threshold itself. The system's own indicator begins to fade in on an isolated press and dies before it completes; repeated sub-threshold presses do raise it solidly. **Released-under-threshold closes the microphone at the release, not at the end of the second-tap window** (Anthony) — nobody speaks during a double-tap, and it halves the worst case. What lost: arming at the threshold instead, which is where the 54 ms would have stayed. | Extends §4.1 and §4.8 | No |
| 2026-08-24 | 3 | **The capture tap is installed once per input device and left in place across recordings, and `engine.prepare()` runs on the stop path rather than the start path.** `AudioCapture.arm()` reinstalls only when the pinned device or its format changes, so a device change still cannot drop an in-flight recording (§4.8). | Measured 2026-08-24. Installing the tap cost **27 ms** and `prepare()` **14 ms** on every single recording; both collapse to **0.4 ms** when the tap survives. Removing them alone made things *worse* — `engine.start()` rose 36 → 68 ms, because `engine.stop()` releases what `prepare()` allocated and the next `start()` re-did it inline. Re-preparing after `stop()`, which is off the latency path entirely, brought it to 55 ms. Net engine bring-up **77 → 55 ms**. The residual ~55 ms is hardware and has no further lever. | Extends §4.8 | No |
| 2026-08-24 | 3 | **`AVAudioEngine` ignores the tap's `bufferSize`, so the requested 4096 frames is not a tuning knob and shrinking it was rejected.** The number stays only because the API requires one, and its comment now says so. | Measured 2026-08-24: 4096, 1024, 512 and 256 all deliver **4800-frame buffers** — 100 ms at 48 kHz. The device's own IO buffer is 512 frames (range 15…4096), so the 4800 is `AVAudioEngine`'s own tap buffering, above the device. **Buying that 100 ms back would mean capturing from an `AudioUnit` directly instead of `AVAudioEngine`** — a rewrite of the capture layer, not a parameter change, and it is raised here rather than done. Until then, `first-audio-buffer` cannot fall below `engine.start()` + one 100 ms buffer, which is exactly what the 156 ms warm figure is. | Extends §4.2 | No |
| 2026-08-23 | 3 | **Sotto warms the HUD window, the cleanup model, and the audio input node half a second after launch, rather than on the first gesture.** `HUDPanel.warm()` builds the panel and renders it once at zero alpha; `Cleanup.prewarm()` moves from the gesture to launch; `AudioCapture.warm()` resolves the default input device and brings the input node up. **The microphone is not opened** — no IO is started, verified with `kAudioDevicePropertyDeviceIsRunningSomewhere`, which reads 0 after launch and 1 only during a real dictation. The audio warm-up is **guarded on the microphone grant Sotto already holds**, so a first run still asks at first use of the feature (§2.4) and warms nothing. | **Measured, and it overturns the finding this session started from.** The prior instrumentation attributed the cold cost to `AVAudioEngine.start()`; per-line marks say otherwise. Cold medians over 5 quit/relaunch cycles: reaching CoreAudio for the default device **50 ms**, first `engine.inputNode` access **259 ms**, `engine.start()` **174 ms**, building the HUD window **107 ms**, first render **241 ms**, first contact with the cleanup model **43 ms**. All but `engine.start()` are first-touch costs with no reason to sit on the gesture. After the change, cold **capture-started 904.3 → 354.3 ms** and **HUD on screen 1153.0 → 402.6 ms**, against a warm path of 356.4 / 369.5 — the first dictation after launch now costs what a repeat one does. `engine.start()` fell too, 174 → 46 ms, because bringing the input node up early also brings the device path up. What lost: `engine.prepare()` at launch, which was the suggested fix and addresses in-process allocation — the part the idle regression already proved was not the expensive one. **On the main actor with a 0.5 s delay, not a background queue**, because `start()` runs on the main actor and `AVAudioEngine`'s graph is not safe to mutate from two threads. | Extends §4.8; supersedes `rules/audio-and-transcription.md` §3.1's "prewarm when the gesture arms" | No |
| 2026-08-23 | 1 | **The main window keeps §10.1/§10.2's shape exactly: `.accessory` at launch, the Dock icon owned by the window, `Cmd+W` returning Sotto to the menu bar. Two alternatives were built, measured, and rejected.** | Anthony, 2026-08-23, after trying both. **What this leaves unfixed:** the window can appear on top of a full-screen app's Space instead of switching to Sotto's own. Measured — the window is level 0 with `collectionBehavior == 0`, so it is not a window-level or window-flags problem; setting both explicitly changed nothing and was reverted. The cause is the process: an `.accessory` app owns no Space, so a window it orders front lands on whichever Space is active. **Rejected fix 1, making Sotto permanently `.regular`:** verified to fix it — activating Sotto from a full-screen TextEdit Space switched Spaces correctly — and rejected because it puts Sotto permanently in the Dock. *"Sotto lives in the menu bar… if the actual window isn't open, the app should just live in the menu bar and stick to just the overlays."* **Rejected fix 2, opening the window at launch:** flipping `.accessory`→`.regular` inside one launch cycle leaves the Dock icon up with no window on screen. Anything future has to keep the menu-bar-first shape. | — | No |
| 2026-08-23 | 6 | **`Transcribe File…` moves out of the Audio detail pane and into the Audio sidebar**, in the slot slice 10's New Chat will take. | Anthony, 2026-08-23. In the detail pane it drew beside the pin on whichever recording was selected, which reads as an action *on that recording*; an import creates a new entry instead. It is the twin of New Chat, so it takes New Chat's position. What lost: §10.2's own placement, which lists it among the Audio mode's detail controls — and the 2026-08-20 note that it sits in the header row or the empty state's action slot; both are now gone, because one action wants one home. Still a disabled stub with the §14.7 tooltip until slice 14. | §10.2 | No |
| 2026-08-23 | 5 | **The audio ring defaults to 10 recordings, not 8.** | Anthony, 2026-08-23. No reasoning offered against the 8 and none needed — §9.2 calls the count configurable, and the default is a preference, not a constraint. `AudioHistory.defaultRingLimit`. Nothing else depends on the number: §4.3's calibration job reads more entries, not fewer, and imports are auto-pinned either way. | §9.2, §10.3's "default 8-recording ring" | No |
| 2026-08-20 | 6 | **The recording list search field sits above the Chat/Audio mode pill, not inside the mode's own list.** One field, and the mode switches the *binding* rather than the field's presence. | Not a taste decision — `.searchable(placement: .sidebar)` renders at the top of the sidebar column no matter how deep in the hierarchy it is written. A per-mode field therefore appeared and disappeared above the pill, and the pill jumped by the field's height on every mode switch. What lost: putting the field under the pill where the drawn order would suggest, which SwiftUI silently ignores. Settings is a page rather than a mode and has nothing to search, so it gets no field at all. | Extends §10.2 | No |
| 2026-08-20 | 6 | **Raw is the transcript pane's default side, and clicking a word seeks only there.** The Cleaned segment stays visible but disabled until slice 11 writes `cleaned`, with the reason in its tooltip. | `words` indexes the raw transcript — the timings come from `SpeechTranscriber`'s runs over what it actually heard, and cleanup rewrites that text without re-timing it. Defaulting to Cleaned would make the pane's central interaction dead on arrival for the side the user is looking at. What lost: hiding the Cleaned control until it works, which is §14.7's rejected pattern — a control the user can see with the reason in a tooltip beats one that materialises later. | Extends §9.3 | No |
| 2026-08-20 | 6 | **`waveform.playbackHeight` = 48 pt, a tier-2 authored token, and a fourth entry outside §9's three pre-approved ones.** Its four claims: role `waveform.playbackHeight`; first consumer `PlaybackWaveform`, slice 6; rejected system value `NSLevelIndicator`, which reports one current level rather than a whole recording's shape and was already rejected for the HUD on the same ground; added slice 6. **`barWidth` and `barGap` are reused from the HUD waveform rather than re-authored, and the bar *count* is derived from the measured pane width — so the surface adds one number, not four.** | There is no system metric for the height of a drawn waveform because macOS does not draw one; Voice Memos' is custom too. **Anthony should rule on this one — it is the fourth authored entry and §9 says tier 2 is permanent debt that should approach zero.** Judge it at real size rather than from the number: 48 pt is a little over two body line-heights. | Extends §14.2's token sheet | No |
| 2026-08-20 | 6 | **`text.transcript` is a tier-1 token resolving to `.body`**, and the "tuning" §14.4 anticipates for transcript type is deliberately not authored. | A tier-2 row costs permanent debt and needs evidence that a system value fails. There is none yet — nobody has read a long transcript in this pane. What lost: `.title3` and a fixed point size, both of which would have been a guess dressed as a decision. If reading a real import shows `.body` is wrong, that is the evidence, and the row can move to tier 2 then. | Extends §14.2's token sheet | No |
| 2026-08-20 | 6 | **The playback transport is a waveform of the whole recording, not a scrubber track.** Click or drag anywhere on it seeks; the played portion draws in the accent colour. | Anthony chose it against a plain scrubber. `rules/design.md` §1's first test is what does macOS do here, and the answer is Voice Memos, which draws the envelope. The envelope also earns its pixels twice — it is the only thing on screen that shows *where the speech is*, which is what makes "find the moment you said a word" work when the transcript is wrong about a word. What lost: a scrubber, which is cheaper (no decode) and which I initially recommended on that basis; the decode cost is a detached read of the file that is already on disk, and weighing it above the system's own answer was the wrong order. | Extends §9.3 | No |
| 2026-08-20 | 6 | **Pause markers are stripped for display.** `[pause Nms]` stays in `entry.raw` on disk and comes off in the pane, via `AudioHistory.unmark` sitting next to the `mark` that writes it. | Anthony's call. The markers exist for cleanup's benefit (§4.6), not the reader's — a transcript reading *"So the thing is, [pause 780ms] I think we should ship"* is Sotto showing its working. What lost: rendering them as a visible glyph or a gap, which is a design for a problem nobody has. The sidecar is unchanged and still inspectable, which is the point of §9.2's on-disk format. | Extends §4.6, §9.2 | No |
| 2026-08-20 | 6 | **A recording can be deleted by the user, from the sidebar's context menu, behind a confirmation.** Deleting removes the whole folder — `audio.caf` and `entry.json` together. | Anthony's call. §9.2 specifies the retention ring and the pin flag, which between them are the *only* way anything left the store — so a user who dictated something they regret had to wait for eight more dictations to evict it, or find the folder in Finder. The confirmation is there because the ring is silent and reversible-by-pinning while this is neither. **Reveal in Finder sits beside it**, since the format being inspectable (§9.2) is worth nothing if there is no way to get to it. | Extends §9.2 | No |
| 2026-08-19 | 5 | **Obsidian is not a feature.** Chats are still one folder with `chat.md` and `attachments/` because images, tool calls, and per-turn metadata do not fit in a single file. That format happening to open in a vault is an accident, not a success test, and Sotto is not changed to make a vault prettier. | Anthony: *"I want to make it so Obsidian isn't a feature. If it works by default then great but I dont want to make changes to Sotto just so Obsidian works. We just need a way to save data."* What lost: treating "drops into Obsidian and reads correctly" as Slice 5's Done when. Proof is a file a text reader can open. | §9.1's "Drops directly into Obsidian"; build-order slice 5 **Done when** | No |
| 2026-08-19 | 4, 5 | **The leftover of slice 4 — `SpeechDetector` pause markers and keeping the PCM — lands in slice 5, not as a fake slice 4.** The audio chunker stays skipped. History stores Opus-in-CAF, the marked raw transcript, word starts, pauses, and empty `cleaned` / `profile` / `languages` slots for slice 11. The chat-folder writer ships with no caller. | Slice 4 collapsed to wiring the analyzer's streaming input, which slice 3 already did. Pause storage was still load-bearing for §4.6 and §9.2, and capture was discarding the buffer the Opus encode needs. Folding both into history keeps one PR for "dictation writes something you can open," instead of a slice whose only remaining job is a prerequisite. | Build-order slice 4 "Store every pause"; slice 5's chat Done-when as an Obsidian test | No |
| 2026-08-19 | 3 | **Insertion prepends a space when the caret sits hard against the previous word.** The character before the caret is read through `AXSelectedTextRange` plus `AXStringForRange`; a space is added unless that character is whitespace or an opener (`(`, `[`, `{`, `<`, `"`, `'`, and the curly quotes). Computed once before the ladder picks a rung, so the paste path gets the same answer as the AX path. **The clipboard path never gets one** — a leading space in what the user pastes themselves is litter. | Anthony, from use: dictating with the caret straight after a word produced `wordThe transcript`. The transcriber has no idea what is on the other side of the insertion point, and cleanup makes it worse rather than better — it capitalises a first word, so the joint reads as a typo rather than a missing space. What lost: doing it in cleanup's instructions, which cannot work because cleanup never sees the target field; and always prepending, which double-spaces the common case of dictating after a space you just typed. **No read means no space**, which is the safe direction — a missing space is one keystroke, a spurious one is an edit to text the user did not touch. Verified in TextEdit across all three cases: after `o` → space, after `(` → none, after ` ` → none. | Extends §3 | No |
| 2026-08-19 | 3 | **Before treating a missing focused element as §3's no-focused-field case, Sotto sets `AXManualAccessibility` on the frontmost app.** One call on the failed read, fired when the gesture arms so the tree is up long before `insert()` asks again; unsupported apps fail it harmlessly. **§3's ladder and its clipboard rule are unchanged** — this is what stops the clipboard path being reached by accident. | **Chromium ships its accessibility tree switched off, so an Electron app has no focused element at all — not an unwritable one, none** — and Sotto correctly read that as "no field focused" and copied to the clipboard instead of inserting. Measured against T3 Code, 2026-08-19: `AXWindows` empty and `AXFocusedUIElement` returning `kAXErrorNoValue` while a text box was focused and typing into it worked; after the one set call, the same element reports `AXTextArea` with `AXValue` settable, `AXSelectedTextRange`, `AXNumberOfCharacters`, and `AXStringForRange`, which is everything both rungs and the space rule need. What lost: `AXEnhancedUserInterface`, which also switches the tree on but is VoiceOver's flag and makes some apps change window behaviour; and doing nothing, which cedes every Electron app — VS Code, Slack, Discord, T3 Code — to the clipboard. **This is a fix to reach the specified behaviour, not a change to it.** | — | No |
| 2026-08-19 | 3 | **The Accessibility write's return status is not evidence that it happened, so the insertion ladder measures the write instead of trusting it.** `AXNumberOfCharacters` is read before and after; unchanged means the write did not land and paste takes over. **§3's ladder is unchanged in shape — two strategies, in the same order — but the rung between them is now a measurement rather than an error code.** | **Safari answers `kAXErrorSuccess` to an `AXSelectedText` write on a plain `<textarea>` and does nothing with it.** Measured 2026-08-19: focused element reports `AXTextArea`, the set call returns 0, the field stays empty, and the transcript is gone with no error anywhere on screen — the one outcome §4.5 exists to prevent, and it is the *third* of the build order's three targets, so believing the status ships a dictation app that cannot dictate into a browser. What lost: reading `AXValue` before and after, which is the obvious check and copies the whole document across the AX boundary twice per dictation when the document can be a book. `AXNumberOfCharacters` is an integer and both AppKit and WebKit maintain it. **The known hole is a same-length replacement, and it cannot arise here** — a selection routes to chat (§4.9), so insertion always runs against a bare caret. Verified after the change: TextEdit and Notes still take the AX rung, Safari takes paste, and Notes still undoes in one `Cmd+Z`. | Extends §3 | No |
| 2026-08-19 | 3, 4 | **A `SpeechTranscriber` or `DictationTranscriber` module belongs to one `SpeechAnalyzer` for its lifetime. Sotto builds a fresh module for every recording and releases it at teardown.** The locale and the resolved audio format survive between recordings; the module does not. | **Reusing one module across two analyzers traps inside the framework and kills the process.** `EXC_BREAKPOINT` at `TranscriberCommon.worker.setter` ← `SpeechAnalyzer.setWorkers` ← `prepareModulesIfNeeded()` ← `processInput(_:)`, two crash reports with identical stacks, reproducing on the **second dictation of every launch**. This is not the intermittent FoundationModels trap logged below — it is deterministic, has a named trap site in Speech, and has a fix. What lost: holding one long-lived transcriber as an optimisation, which is what the code did until it crashed. Rebuilding costs nothing measurable against the 0.507 s to first result, and the constraint is worth stating outright because the reuse is the natural thing to write. | Extends §2.2, §4.7 | No |
| 2026-08-19 | 3 | **Sotto asks for the microphone through `AVCaptureDevice.requestAccess` and never by letting `AVAudioEngine.start()` raise the prompt. The gesture that triggers the question is spent and the HUD says nothing; the next gesture records.** A *denied* microphone says **"Microphone access is off"** on the HUD. | **`engine.start()` blocks its caller until the TCC dialog is answered — measured at 45 s on this build's first run — and the caller is the main actor**, so the menu bar, the status item, and the HUD freeze behind a dialog the user is still reading. §2.4's "ask at first use of the feature that needs it" is untouched; only the mechanism changed. **The HUD stays silent for the pending case on purpose:** the question is on screen, and an error morph behind it is Sotto complaining about something the user is in the middle of answering. **The denied case is the part worth arguing about, and it is put to Anthony rather than settled here** — by decision 42's own logic a denied microphone is a configuration state, knowable before the gesture fires, with its remedy in System Settings, which is the argument that moved cleanup-unavailable off the HUD. It is on the HUD today because slice 3 has no Dictation settings pane to route it to, and because unlike a model the microphone is what the gesture is *for*. | Extends §2.4; sits against decision 42 | No |
| 2026-08-19 | 3 | **Sotto never downloads a locale's speech assets. If `status(forModules:)` is not `.installed` after `reserve()`, transcription is unavailable and the HUD says "Language not available."** | §2's consent rule: every outbound connection is something the user just did or something the user switched on, and dictating is neither — the user asked to dictate, not to fetch a model. 17 of `SpeechTranscriber`'s 30 locales ship with macOS and the other 13 are real downloads, so this is a live path, not a theoretical one. **`downloadAndInstall()` is deliberately not called anywhere in slice 3.** What this leaves open, and it is a genuine gap rather than an oversight: a user on one of those 13 locales gets a dictation app that does not dictate and a four-word HUD string that does not tell them what to do about it. The surface that offers the download — and it must be a surface with a button the user presses, per §2 — belongs to a settings pane slice 3 does not build. | Extends §2, §4.7 | No |
| 2026-08-19 | 2, 3 | **FINDING, not a decision — macOS's own dictation also answers a held Right Command on the reference machine, so §4.1's primary gesture is contended out of the box.** Reproduced with Sotto quit: holding Right Cmd still raises the system dictation surface. **Nothing in Sotto changed for this**; the test harness moved to the double-tap latch, which the system does not claim. | Recorded because it is the kind of thing that reads as a Sotto bug for the rest of the project — the first symptom was text appearing twice, which looked exactly like a duplicated insertion until the app was quit and the text still appeared. §4.1's hold is not broken and the double-tap latch is unaffected; what is at stake is that a user who has never opened System Settings gets two dictation UIs on one gesture. **Anthony's call, and it is a real one:** ship as is and let the shortcut be the user's to change, detect the conflict and say so at onboarding (slice 15), or move the hold. Not decided here because §4.1's gesture is his and this is a behaviour a user notices. | Bears on §4.1 | No |
| 2026-08-19 | 3, 4 | **Apple's Speech framework is the exclusive v1 STT backend. Parakeet, FluidAudio, Silero, and Whisper do not ship in v1 in any form.** A non-Apple backend stays possible in a later version and is explicitly not part of the current architecture — §2.2's "audio in, timestamped text out" interface is what keeps that door open, and nothing in v1 is built to receive a second engine. **This closes the clause the STT row below left open.** | **Apple ships its own fallback and it is wider than the engine it would have been paired with.** `DictationTranscriber` reports **54 supported locales** against `SpeechTranscriber`'s 30 — Russian, Dutch, Hindi, Arabic, Polish, Thai, Vietnamese, Ukrainian among them — and every locale probed where `SpeechTranscriber` returned `.unsupported` came back `.supported` there. It keeps what §9.3 needs: `.audioTimeRange` and `.timeIndexedLongDictation` are real, and **23 of 23 runs carried timings**, so click-to-seek survives the fallback path. **What it does not carry is punctuation or mid-sentence capitalisation** — on the same clip `SpeechTranscriber` returns "So the thing is, I think we should ship the smaller model first." and `DictationTranscriber` returns the same words unpunctuated — and **cleanup already supplies exactly that**, so the gap lands on a stage Sotto was running anyway. Whisper's remaining advantage is the ~99-locale tail, which does not buy a second engine, a second weights story, and the 30 s chunking stage §4.2 was just relieved of. **The accuracy figure is a floor and is not a Whisper comparison:** WER **1.71 %** (26 errors in 1518 words — 18x `and`→`end`, 7x `first`→`1st` which is Apple's ordinal normalisation rather than an error, 1x `first`→`fur`) on `say`-synthesised audio carrying no noise, accent, or disfluency. **Incidental and load-bearing for slice 11:** Apple normalises ordinals to digits, so cleanup's instructions are writing against text that already has house formatting. | The "whether any non-Apple STT backend ships in v1 is **undecided**" clause of the STT row below; §6's Whisper large-v3-turbo alternate | No |
| 2026-08-19 | 4, 9, 14 | **Realtime microphone dictation may overlap Foundation Models freely. File and import transcription is serialised around active chat generation rather than competing with it.** | **Both STT and Foundation Models execute on the ANE. Neither touches the GPU.** Measured with `H11ANE` `IOPowerManagement.CurrentPowerState`, the `H1xANELoadBalancerDirectPathClient` count, and `IOAccelerator` `Device Utilization %`: STT powers the ANE for **96.2 %** of its window and takes it from 4 clients to 6 while the GPU sits at 2.9 % against a 4.1 % idle baseline; Foundation Models holds the ANE at **100 %** with 6–8 clients and the GPU flat at 1.2 %. **That inverts §2.2's reason for choosing Parakeet over Whisper** — the ANE was picked precisely because it does not contend with an LLM on the GPU, and under the all-Apple stack both live on the ANE instead. **Sharing the block only costs at import rate.** Against contemporaneous controls — the Foundation Models baseline drifted 841 → 682 ms across the session, so single-run comparisons were discarded — microphone-rate STT at 1x realtime puts cleanup at 719 ms against a 682 ms control, **+5 %, inside run-to-run drift**; file-import STT at 60x realtime puts it at ~1200 ms, **+76 %**, and drags STT itself from 76.1x to 57.5x, **−24 %**. Dictation and cleanup are sequential anyway, so the case this actually governs is an import running under a chat response — which is why the serialisation is placed there and nowhere else. **Bounds, so this is not oversold:** one machine (A18 Pro, 8 GB, macOS 26.5.2); no `sudo`, so this is ANE occupancy and latency rather than per-block power; ANE power-down has hysteresis, so "powered" windows overhang the work that caused them and the client-count delta is the cleaner signal. | §2.2's ANE-versus-GPU argument as it applies to the Apple path; `rules/audio-and-transcription.md` §1.0's "open and unmeasured" | No |
| 2026-08-19 | 9, 11 | **Concurrent Foundation Models work requires one `LanguageModelSession` per operation — cleanup and chat each own theirs, and neither reuses the other's.** There is **no parallel speedup**: concurrent requests serialise at the model level, so overlapping cleanup with a generating chat response costs roughly a full request on whichever the user is waiting for. **The intermittent trap below is logged as a robustness investigation item and does not reopen the architecture.** | **Tested, which the `rateLimited` row below explicitly left undone.** Two distinct sessions fired simultaneously both completed with zero errors, and that held to **six concurrent sessions across twelve rounds** — no `concurrentRequests`, no `rateLimited`. **Reusing one session for two simultaneous requests throws `concurrentRequests` deterministically**, 4 of 4 trials, one arm failing while the other completes. Serialisation shows in the totals: concurrent 889–1231 ms against sequential 1025–1106 ms for the same pair, and n scales flat — 2 → 345 ms, 4 → 750 ms, 6 → 1030 ms. **Uncharacterised, and deliberately not overstated:** an intermittent `SIGTRAP` inside FoundationModels itself, twice in ~13 runs under concurrent-session load, identical trap site (`EXC_BREAKPOINT`, `FoundationModels+269145 → +317xxx`, on a Swift concurrency thread), **not reproduced in eight subsequent runs**, no identified trigger. It matters because a trap kills the process rather than surfacing as an error `rules/design.md` §10 could route. Two occurrences are not a characterised API failure and are not grounds to abandon separate-session concurrency; they are grounds to look again before slice 9 ships. | The `concurrentRequests` **untested** bound in the `rateLimited` row below | No |
| 2026-08-19 | 3, 4 | **STT moves to Apple's Speech framework — `SpeechAnalyzer` + `SpeechTranscriber`. Parakeet, FluidAudio, and Silero come out of the default path.** Anthony: *"I still think we should use the speech framework though just to make this possible 100% with apple."* **Undecided and deliberately not logged here: whether any non-Apple STT backend ships in v1.** | **The models are already on the machine — ~1.0 GB of system ASR assets — and `AssetInventory.reserve(locale:)` claims them with no download.** `.supported` meant "not claimed by this app," not "not present"; `installedLocales` is not a safe gate and disagrees with `status(forModules:)`. Measured on the reference machine: **69× realtime**, first finalized result at **0.507 s**, **95 MB flat** over 9 minutes, per-word timings at ~60 ms with start and end, native punctuation and capitalisation, 30 locales supported and 17 installed against Parakeet's two. `SpeechDetector` is preinstalled, which is what retires Silero. What lost: Parakeet via FluidAudio, on locale coverage and on Sotto no longer hosting, verifying, or resuming STT weights at all. | §2.2, §4.7, §9.3 | No |
| 2026-08-19 | 4 | **§4.2's audio chunker has no basis on the Apple path and slice 4 collapses to wiring the analyzer's streaming input.** Chunking stops being a pipeline stage and becomes a per-backend detail *below* §2.2's "audio in, timestamped text out" interface. **§4.6's cleanup *text* chunker is untouched and matters more, not less** — the two are different chunkers and the spec makes them easy to conflate. | `maxChunk = 240,000 samples` is not a chosen number: §4.2 states outright it is FluidAudio's internal `ChunkProcessor` threshold, and the whole overlap-and-reconcile design exists to stay under it. No FluidAudio, no threshold. Measured: the analyzer emitted **110 finalized results over 9 minutes, segmented and streamed unprompted**, at flat memory — so the latency argument for chunking (§4.2's real one, time-to-first-token) is already served at 0.507 s. **The ~0.17 % seam-loss baseline and the WER comparison programme both leave v1 with the chunker they were measuring.** Whisper's 30 s receptive field would still require chunking, but that is the *model's* constraint and belongs in a Whisper backend, not in the pipeline. | §4.2, §10.3, and slice 4 of the build order | No |
| 2026-08-19 | — | **Parakeet needs CoreML, not FluidAudio — and not MLX.** If Parakeet ever returns, FluidAudio is one packaging among several (`soniqo/speech-swift`, `Blaizzy/mlx-audio-swift`); what is actually required is CoreML, because CoreML is what targets the ANE. | **MLX has no ANE backend** — it targets Metal and CPU — so Parakeet through MLX puts STT on the GPU contending with the LLM, destroying the single property §2.2 chose Parakeet for. Independently confirmed by the ecosystem: **FluidInference built `swift-parakeet-mlx` and abandoned it**, reporting that performance on Apple devices was inadequate and shifting back to a CoreML Parakeet. Recorded so the "just run it through MLX" question is not re-argued. | Clarifies §2.2 | No |
| 2026-08-19 | 3, 11 | **Cleanup-model failure splits in two, and only one half is the HUD's.** *Unavailable* — `SystemLanguageModel.availability` is not `.available` — is a configuration state, knowable before the gesture fires, and routes to the settings model picker with a banner. *Failed mid-pass* stays on the HUD. **Slice 3 designs the error morph without a model-unavailable string.** | §14.3 and `rules/design.md` §10 route "audio or cleanup model fails to load" to the HUD as one case. That was written when every cleanup model was a file Sotto downloaded, where load failure is genuinely a runtime event mid-pipeline. Apple's model cannot fail to load — it is either enabled on the machine or it is not, and the remedy is in System Settings, not in the dictation the user just spoke. A HUD morph that says "cleanup unavailable" over and over on every dictation is a notification in all but name, which §2 rules out. What lost: keeping the single row and letting the HUD carry both. | §14.3, `rules/design.md` §10 | No |
| 2026-08-19 | 3, 11 | **Cleanup prewarms when the gesture arms, and prewarming never contributes to not-idle.** `LanguageModelSession.prewarm(promptPrefix:)` fires on gesture arm; `Activity.Contributor.cleanup` is set only when a cleanup pass actually runs. Slice 3 leaves the hook even though slice 11 fills it. | **Measured 2026-08-19 on the reference machine, 120 requests across three runs:** first request after launch costs ~3.5 s, requests after it settle to ~850 ms median (min 756, max 3358). Un-prewarmed, the first dictation of every session pays 3.5 s in the gap between speaking and seeing text — the one place §12 issue 4 already identifies latency as decisive. The seam belongs in slice 3 because the gesture is slice 2/3 and cleanup is slice 11; without it slice 11 reaches back into gesture handling. Prewarm is excluded from `Activity` because the icon reports that Sotto is awake (§14.8) and a speculative warm-up the user did not ask for is not that. | Extends §4.6, §14.8 | No |
| 2026-08-19 | 11 | **`rateLimited` does not fire for a never-frontmost accessory app, and being background costs no latency.** The risk flagged against the Foundation Models default is not real in this shape. | **Two-arm test, both arms 40/40 requests with zero errors of any kind.** Accessory arm never frontmost (`selfIsFront` 0/40, verified per request rather than asserted); frontmost control 40/40. Medians 858 ms accessory against 838 ms frontmost — a 1227 ms first-run accessory median did not reproduce and was load, not policy. **Bounds, so this is not oversold:** burst only, 120 requests in three ~40 s windows, so a longer-window quota would not show; `concurrentRequests` untested and is a real case now that cleanup and chat share one model; ad-hoc-signed probe rather than Sotto's bundle; one machine, English locale. | — | No |
| 2026-08-19 | 9, 11 | **Both dictation cleanup and chat default to Apple's on-device Foundation Models (`SystemLanguageModel.default`), so Sotto works at first run with nothing downloaded. When `availability` is not `.available`, Sotto opens the model picker in the relevant settings subsection — Dictation for cleanup, Chat for chat — with a banner naming the reason (`deviceNotEligible`, `appleIntelligenceNotEnabled`, `modelNotReady`).** | **Working out of the box with zero downloads is the goal, and it is §7.4's own argument — "it made the app harder to start using" — applied to the default rather than to hand-placed weights. Today the default cleanup model is whatever chat model the user downloaded, which puts a download in front of the headline feature.** What lost: MLX-as-default on both paths, for that reason; and "no chat default, ask the user to pick on first use," which Anthony proposed and withdrew in the same message because it reintroduces a download before the first chat. The fallback is not a new pattern — §7.2 already says features stay present and the user switches models to enable them. **Accepted costs, stated so they are not rediscovered:** 4096 total context (prompt + output) on both paths against §7.3's context slider; `vision == false`, so §7.2 gates image attachment and §5.6 screenshot mode on a fresh install; the model is unversioned from Sotto's side and moves on OS updates. | §2.2, §4.6, §7.3, §7.4, §8.1 | No |
| 2026-08-19 | 3 | **The HUD is a true capsule — `radius = height / 2 = 18` — and the superellipse is given up to get it.** Anthony asked for "a pill but a squircle/superellipse"; the two are mutually exclusive at this size and he ruled for the pill on the renders. | **Rendered on the real screen at 16, 17.5, and 18 before ruling.** §4.1's measured fact is that `style: .continuous` only does anything while the radius is strictly under half the smaller dimension, because the superellipse needs a straight edge to blend into. At 16 the HUD has ~4 pt of straight run per end and reads as a rounded rectangle, which is the complaint that opened this. At 17.5 it reads as a pill but has half a point of straight edge left, so calling it a superellipse is true and meaningless. There is no radius that is visibly both. Both references he has pointed at — Control Center's tile and Superwhisper's pill — are true capsules. | `HUDView`'s "the surface is a squircle, not a capsule" note and the earlier `radius = 16` | No |
| 2026-08-19 | 3 | **A fourth tier-2 token lands, outside §9's three pre-approved: `specular.width/highlight/shadow/rim` — a 1 pt `strokeBorder` carrying an `AngularGradient`, bright white at top-left and bottom-right, weak black at top-right and bottom-left.** Anthony asked for it by name against a Control Center reference. **Its strength is provisional** — `highlight` is at `white.opacity(0.9)` and reads closer to chrome than to Apple's rim. | **Rejected system value: none exists.** `Glass` has exactly five members and none is a knob on the specular pass; `grep -i specular` across the installed SDK returns OpenGL, RealityKit, and SpriteKit and nothing in SwiftUI or AppKit. Real glass does draw its own specular edge, but measured on screen at 36 pt over a light backdrop that edge does not give the surface a boundary, which is the case that produced the row. `.separatorColor` was the nearest semantic value and fails on kind rather than strength — a separator is one flat colour the whole way round, and this is a lit bevel, which is the whole of what Anthony corrected me on. The 180° period is why: one light source above-left, caught twice on opposite edges. | §9's "only these three tier-2 entries are pre-approved" — this is the fourth, claimed aloud | No |
| 2026-08-19 | 3 | **The specular stroke goes on `GlassEffectContainer`, not on the glass view inside it.** Applied inside, the stroke renders diffuse and bleeds inward; moved outside, it is crisp. | Found by rendering the rim in saturated red/green at 3 pt when a 0.55 → 0.9 opacity change produced no visible difference. The waveform in the same frame stayed crisp throughout, which is what ruled out the capture path and the build being stale and pointed at the glass compositing anything layered on it. Not a `clipShape` in either position — §6.1's rule that clipping cuts the edge the material draws outside the path still holds. | Nothing — new | No |
| 2026-08-19 | 3 | **The HUD's appearance is decided once, when it appears, and held until it goes away — and there are exactly two versions, light and dark.** A state morph part-way through a showing does not re-decide it, so the "Copied to clipboard" step cannot itself cause the flip. Implemented as an explicit `colorScheme` on `HUDView`, set by `HUDPanel` at show time. | **Measured, both halves.** Left to itself Liquid Glass re-reads the luminance behind it every frame and inverts its labels with it: one `HUDView`, never touched, with only the backdrop swapping light → dark underneath it, went from black text to white. That is correct for a static surface and wrong for this one — a HUD that inverts mid-dictation because the user scrolled a dark block under it draws the eye at the one moment it should be furthest from mind. An explicit `colorScheme` overrides the appearance the glass hands its content while leaving the *surface* free to tint and refract, which is the material doing its job and was never the thing that needed to hold still. | `rules/design.md` §6.7's "stay legible without a branch — do not add one", now qualified in place | No |
| 2026-08-19 | 3 | **`material.hud` is `Glass.clear`, not `.regular`.** Anthony's ruling on the render comparison, reversing the row logged earlier the same day. | `.regular` was chosen as the inherit-the-default option, with `.clear` argued to be authoring. He overruled it on what the two actually look like: `.clear` refracts visibly harder, and at 36 pt tall the refraction is the whole difference between reading as glass and reading as a grey pill. The legibility objection that motivated `.regular` is answered by the adaptation above — the surface still lightens over a dark backdrop and darkens over a light one. | The `material.hud` = `.regular` row logged 2026-08-19 | No |
| 2026-08-19 | 3 | **OPEN — Anthony asked for the pinned appearance to be sampled from what is behind the HUD; that needs the Screen Recording grant, and `CLAUDE.md` §4 defers Screen Recording to the first screenshot.** Sampling the backdrop means capturing the screen, so dictation would pull that prompt forward to first launch. **Shipped in the meantime: the pin is taken from the system appearance**, which gives the same two versions and the same hold-once behaviour at no permission cost. The seam is one function, `HUDPanel.appearanceNow()`. | Recorded rather than decided because it is a §0.1 tier-3 call — a permission prompt at first dictation is something a user notices, and the alternative has a real failure case: a Dark-mode user typing into a white document gets light-pinned labels over a light backdrop. The `.clear` surface lightens over that backdrop and partly rescues it, which is why the interim is shippable rather than merely expedient. | Nothing yet — this is the queue entry, not the resolution | No |
| 2026-08-19 | 3 | **Three token rows land, all four claims made: `accent.primary` (tier 1) → `.controlAccentColor`, first consumer the HUD waveform's bars; `material.hud` (tier 1) → `Glass.regular`, first consumer `HUDView`; `waveform.count/barWidth/barGap/peak/resting` (tier 2) = 8 / 3.5 / 4.5 / 20 / 3.5, first consumer `Waveform`.** The waveform row is the first of §9's three pre-approved tier-2 entries. **`peak` is provisional and needs Anthony's ruling** — everything else in the row is settled. | `accent.primary`: rejected `.labelColor`, which is what Control Center gives a resting glyph — a resting glyph and a running capture are not the same claim, and the waveform is the only confirmation the user gets that Sotto is hearing them. `material.hud`: rejected `.clear`, which overrides the user's Liquid Glass appearance setting instead of inheriting it; Apple documents `.clear` for surfaces over media that guarantees its own contrast, and the HUD sits over whatever the user happens to be looking at. **Anthony leaned Clear in the render review — this is the tier-1 default pending his ruling, not a decision taken over him.** `waveform.*`: rejected `NSLevelIndicator` on three counts — it is a filled gauge rather than bars, it has no idle treatment other than empty, and it is a control, so it draws a focus ring and a hit region for a surface that has neither. | `sotto-tokens.md` §§1–2's empty state, and §6.2's 12-bar geometry | No |
| 2026-08-19 | 3 | **`rules/design.md` §6 was wrong about three things and is corrected.** `prominentGlass()`, `clearGlass()`, and `prominentClearGlass()` are listed there as available button styles and **do not exist in the SDK**; only `.glass`, `.glass(_:)`, and `.glassProminent` do. `glassEffect`'s default shape is `DefaultGlassEffectShape`, not `Capsule`. The glass API lives in **SwiftUICore**, not SwiftUI. | Re-verified against the installed `SwiftUICore.swiftinterface` rather than documentation, because an earlier session grepped `SwiftUI.swiftinterface` for `glassEffect`, got zero hits, and nearly concluded the API was absent — it is in the other module. A rules file that names three symbols that do not compile is worse than one that names none: the reader spends the search budget on the wrong question. | `rules/design.md` §6.1, §6.2, §6.4 as written | Rules file, fixed in place |
| 2026-08-18 | 3 | **The HUD is the compact form — 91.5 × 36 pt recording state — and its corner radius moves off 12 pt to a continuous radius strictly less than half the height. `r = h/2` is rejected outright.** Anthony picked the compact variant over a Control Center–tile-sized one (139 × 52, measured off the live panel) and specified the shape as a squircle rather than a capsule. **r 16 is recommended; r 14 is the more pronounced alternative.** | **Measured, not assumed:** at `r = h/2` a `.continuous` corner and a circular arc are the same curve. Filled silhouettes of both at 91.5 × 36 were rendered and their edge profiles sampled — inset from the left edge at 3/6/9/12/15 pt down the corner is 7.5 / 4.5 / 2.5 / 1.0 / 0.0 for **both**, identical to measurement precision. There is no straight edge left for the superellipse to blend into, so `.continuous` has nothing to act on. **This matters beyond this one surface:** `rules/design.md` §4.1 forbids circular corners and treats `style: .continuous` as the fix, which is true only while the radius stays under half the smaller dimension. A capsule-shaped surface written as `.rect(cornerRadius: h/2, style: .continuous)` passes §14's grep and still ships a circular arc. Both named references are true capsules — Control Center's tile is r 26 on 52 pt, Superwhisper's pill r 15 on 30 — so choosing a squircle is a deliberate departure from both, on Anthony's instruction. | **§6.2's** HUD radius of 12 pt and its "0.333 H" ratio; the Control Center tile size as a candidate footprint. Adds a constraint `rules/design.md` §4.1 does not currently state | No |
| 2026-08-18 | 3 | **Design.pdf's measurements are no longer locked. Every value in `sotto-tokens.md` §6 is a starting point, not a constraint.** Anthony: *"make it so none of the values are locked anymore starting now."* Read as covering the whole of §6, not only §6.2 — decisions 01, 03 and 04 lose their locked status on the same instruction, though only slice 3's values are being exercised now. | The lock was doing real work when it was applied on 2026-08-11: it stopped four drawn decisions being re-argued every session. It stopped being useful the moment its numbers met a real screen. Four §6.2 rows fell in a single session today — the 8 % anchor, the 12-bar count, the 152 pt floor, the 13 pt string — and each fell for the same reason, that the value was derived against an assumed 1512 × 982 display with a 24 pt menu bar which does not exist on the machine Sotto is built on. **A lock protecting numbers that are wrong converts a measurement error into a standing instruction**, and the failure mode is silent: a later session reads "locked", believes the number was defended, and builds on it. What replaces it is not licence to invent — `rules/design.md` §1's resolution order still applies, tier 2 still has to name the system value it rejected, and every change still gets logged here. **What is lost, stated plainly:** §6 no longer settles anything on its own, so the four decisions it records can be reopened by anyone at any time, and the cost of that is Anthony's to bear rather than mine to prevent. | The **"locked 2026-08-11"** status of `sotto-tokens.md` §6 in full, and `rules/design.md` §11.1's assertion of the same. Design.pdf itself is unchanged and stays the record of what was drawn | No |
| 2026-08-18 | 3 | **The HUD's completion string drops from 13 pt to 12 pt, and 12 pt is `NSFont.preferredFont(forTextStyle: .callout)` — a named system style, so it stays tier 1 and authors nothing.** Measured on macOS 26: `.body` is 13.0, `.callout` 12.0, `.subheadline` 11.0, `.footnote`/`.caption1`/`.caption2` all 10.0. | Anthony asked for a smaller string to go with a smaller HUD. **The incidental finding is that 13 pt was never an authored size at all — it is exactly `.body`**, so the token sheet §3's warning that the HUD's 13 pt "will need its reason stated when it converts" is unnecessary: there is no fixed size to defend, and there still isn't at 12. `.subheadline` at 11 was the alternative and lost on legibility — a confirmation that is on screen for about a second should not be fine print, and 11 pt is where a transient message starts asking to be squinted at. The locale table survives the change with room to spare: en 143 pt, de 201, fr 198, es 187, nl 182, ja 203, all far under the 320 pt cap, and the single-line invariant holds in every one. | **Token sheet §3's** "the HUD completion string at 13 pt" and its note that three fixed sizes will need reasons; **§6.2's** measured locale width table, every row of which was taken at 13 pt | No |
| 2026-08-18 | 3 | **The waveform drops from 12 bars to 8, and the HUD gets two widths instead of one: a recording width of 91.5 pt driven by the waveform, growing to a message width of 143 pt driven by the string.** Bar width 3.5 pt and gap 4.5 pt are unchanged, so 8 bars is 59.5 pt of waveform. **The 152 pt width floor is deleted.** | Anthony, having named Superwhisper's Mini HUD as the reference he likes: *"can we make the font a little smaller and have the waveform be smaller and just grow when we have the copied to clipboard. Also we can use less bars too, maybe 8 instead of 12."* Measured, that reference is a **60 × 30 pt** pill with **6 bars**; 8 keeps Sotto slightly denser without matching it. **The floor had to go because it was never the waveform's floor** — §6.2 describes 152 pt as "the floor the waveform needs", but twelve bars plus padding is 123.5 pt and eight is 91.5. The 152 came from the English string at 13 pt (119 + 32 = 151, rounded up), so it was the message state's width wearing the recording state's name. Splitting them is what makes §5's existing 220 ms ease-out width change meaningful rather than incidental: the HUD now visibly grows 1.56× at the moment dictation ends, which is exactly when the eye should be drawn. **Cost, stated:** eight buckets of recent envelope instead of twelve is a coarser read on what the voice just did — acceptable because §4.5 gives the waveform one job, confirming the gesture registered and that audio is arriving, not spectral analysis. | **§6.2's** bar count of 12, its "Total width 91.5, 0.60 of HUD width", and its "Width floor 152 = 4.22 H" row; **§4.5's** implication of a single HUD width | No |
| 2026-08-18 | 3 | **The HUD's anchor is a constant distance from the top of `NSScreen.frame`, not a percentage of screen height and not measured from the bottom of the menu bar.** The value is not yet picked; **52 pt is recommended** and three candidates were composited over a real desktop for Anthony to choose from. Whatever the number, it does not branch on whether the menu bar is present. | Anthony: *"I think the HUD should be a constant distance from the top that makes it so that it is low enough to not be uncomfortably close to the menu bar when it is there, even when the menu bar is hidden."* Menu-bar-relative anchoring loses on the very case that motivated it: **remove the menu bar and a menu-bar-relative HUD slides up to hug the physical screen edge**, which is the opposite of what fullscreen needs. It also has four inputs rather than two — fullscreen, the auto-hide-menu-bar setting (which can toggle while the HUD is on screen), the notch, and multi-display — and its payoff is a HUD that appears in a different absolute place depending on which app you happened to be dictating into. A percentage loses separately: **nothing about the HUD scales with the screen** — 36 pt fixed height, 13 pt type in every locale, a width floor in points — so a proportional anchor would push a fixed-size object 115 pt down a large external display for nothing. **The floor on the constant is the notch:** a notched MacBook's menu bar is ~37–38 pt and the notch sits dead centre, exactly where the HUD is, so the anchor must clear ~38 pt with a visible gap — which is why Superwhisper's 33 pt cannot simply be copied. Measured reference points on Anthony's own display (1637 × 1024 pt, 30 pt menu bar): the Superwhisper Mini pill sits at **33 pt**, macOS's own notification banners and widgets start at **~45 pt**. | **Token sheet §6.2's** "Anchor — top edge at 8 % of screen height, centred", from Design.pdf decision 02, and the **HUD half of gap 2** in `rules/open-questions.md` and `sotto-tokens.md` §7 — closes the form, leaves only the number | No |
| 2026-08-18 | 3 | **The token sheet's assumed platform metrics are wrong for the machine Sotto is being built on, and decision 02's 8 % anchor is about one HUD height too low as a result.** Measured: `frame` 1637 × 1024 pt, menu bar **30 pt**, `safeAreaInsets.top` 0, Dock reporting **zero** occlusion (auto-hidden). The sheet assumes 1512 × 982 with a 24 pt menu bar and an 84 pt Dock. | §6 states outright that "every ratio expressed against screen dimensions inherits those assumptions", so this is the sheet's own caveat firing rather than a new problem — but the size of the error is what matters. **8 % of 1024 puts the HUD's top edge at 82 pt**, against 33 pt for the Superwhisper Mini HUD that Anthony named as the reference he likes, and ~45 pt for where macOS places its own notification banners. The locked value is therefore not merely un-decomposed, it is roughly one full HUD height lower than his stated taste — which is why the anchor could not be settled as "value or rule" alone. **The auto-hidden Dock also pre-empts slice 9:** §6.1's overlay clearance rule assumes an 84 pt Dock to clear, and on this machine `visibleFrame` reports none. | **Token sheet §6's** "Assumed platform metrics" block, and by consequence every screen-relative ratio in §6.1 and §6.2 | No |
| 2026-08-15 | 2 | **"The event tap observes modifier keycodes only" is wrong in both documents and becomes: modifier keycodes, plus a bare keycode comparison on ordinary keydowns while a gesture is armed.** The privacy claim it was written to carry — no key content is read, buffered, or stored — is unchanged and stays literally true. | The sentence describes a tap that cannot implement the slice it appears in. Two requirements in slice 2 need ordinary keys: **chord abort** must notice a non-modifier keydown, and **Escape abort** must notice keycode 53, which is not a modifier. Neither is reachable from a modifier-only subscription. What ships instead: the tap subscribes to `keyDown` as well as `flagsChanged`, and on a `keyDown` — while and only while a gesture is armed — asks two yes/no questions, "is this keycode 53" and otherwise "is this any key at all." **No character is ever decoded, nothing is accumulated, and the callback returns immediately when no gesture is armed.** So only the mechanism description was wrong; the promise the sentence existed for survives verbatim. Left uncorrected it would have been read as a constraint by a later session and produced either a broken Escape or a silent workaround, which is the one failure `CLAUDE.md` §5 names outright. | **§2.4's** "The event tap observes modifier keycodes only" and the **build order's slice 2** bullet repeating it. Both keep their second sentence unchanged | No |
| 2026-08-15 | 2 | **Right Cmd is a live modifier for the first 250 ms of a hold and is consumed after it. Past the 250 ms line the gesture owns the keyboard: Right Cmd stops acting as a modifier and every non-modifier keydown is swallowed, Escape alone excepted.** Normal behaviour returns on release, while transcription runs. **A chord under 250 ms disarms**, exactly as Escape does — the speculative capture is discarded, the chord passes through and copies, releasing the key afterwards does nothing, and the key never becomes consumed for that hold. **Latched is unaffected**: no key is held, so Cmd chords work normally throughout a latched recording. | Anthony: *"If I am holding the right command and I am recording, I shouldn't be able to copy and stuff during that… during that time right command is only for recording… Nothing else should happen until I let go."* The spec's unqualified **never consumed** was protecting exactly one case — Right-Cmd+C keeps copying — and **that case survives intact**, because a chord is pressed inside the 250 ms window in practice. What changes is the tail, where the key has already been classified as a gesture and letting it also act as a modifier is one physical key doing two jobs with no way for the user to say which. **The full swallow beat the two narrower options on a mechanical fact, not on taste:** CGEvent flags are computed from hardware state, so suppressing the modifier's `flagsChanged` does not strip Cmd from the keydowns that follow — stripping it converts Cmd+C into a literal `c` typed into the document the user is about to dictate into, which is silent corruption of the insertion target. **The cost, stated and accepted:** left-hand shortcuts die for the length of a hold too, Cmd+Tab and Cmd+S included, because the swallow keys off keydowns rather than off which modifier is down. A push-to-talk hold is seconds long and the user is talking, not typing. **Quit-as-panic (§10.5) is unaffected** — Escape is excepted, so Cmd+Opt+Esc passes through, and it aborts the gesture on the way. | **§4.1** and the **build order's slice 2** bullet, both of which state "Right Cmd is **never consumed**" without qualification. §4.1's state machine is otherwise unchanged; this adds the boundary and resolves what a chord does to the arm state | No |
| 2026-08-15 | 2 | **The overlay gesture is double-tap Right Option only — keycode 61. Left Option is not a trigger, and neither is Left Cmd.** Sotto is a right-hand program: Right Cmd (54) dictates, Right Option (61) opens the overlay. Those are the only two keycodes the tap acts on. | Anthony: *"I want this to just be a right hand program… I think right option would be the right call here. So only right option."* Dictation already lives on Right Cmd, so putting the overlay on either Option key splits the app across both hands for nothing — the left-hand copy is not an accessibility affordance, it is a second way to fire the same thing. Two adjacent right-hand modifiers is a posture the hand learns; "Right Cmd and either Option" is not. **The privacy claim gets cheaper to audit rather than more expensive:** two keycodes acted on instead of four, halving what a reader has to accept. **One precision so the claim stays honest:** a `flagsChanged` subscription cannot be filtered per keycode by the OS, so the callback does see other modifiers and discards them on the next line. "Acts on 54 and 61" is the true statement; "is only delivered 54 and 61" would not be. | **§2.4's** "58/61 for Option" and the **build order's slice 2** bullet repeating it; **§4/§5's** "Double-tap Option" wherever it appears unqualified. §2.4's "54/55 for Right/Left Cmd" narrows to 54 for the same reason | No |
| 2026-08-15 | 0 | **The app icon's layer split is settled, and the asset is in the repo at `Sotto/Sotto.icon`.** Two layers in one group — `1-grey-top` and `2-grey-bottom`, the upper capsule occluding the lower — with the shadow (neutral, 0.5) and translucency (0.5) declared as group properties rather than drawn into the art. `ASSETCATALOG_COMPILER_APPICON_NAME` moves from `AppIcon` to `Sotto`, and the empty ten-slot `AppIcon.appiconset` is deleted. | Anthony built it in Icon Composer. The mark was already locked — two capsules forming an S — so only the split was open, and producing the asset is what closes it: **one layer per capsule is the minimum that lets the system cast the upper's shadow onto the lower**, which is the whole reason the occlusion was specified. What loses is the `.appiconset` path, and it loses on capability rather than taste: an Icon Composer document generates Default, Dark, Clear, and Tinted from the same two layers, a rasterised iconset generates none of them, and shipping both would put one icon that adapts beside one that does not. **`rules/design.md` §6.9's rule survives intact** — nothing is baked. No mask, no blur, no highlight; the shadow and translucency are declared values the system applies, and the two source files are SVG. | Slice 0's *Still open* list and `rules/open-questions.md`'s app-icon entry — **closes both.** Spec §14 is unaffected; the icon was never a token | No |
| 2026-08-15 | 1 | **`surface.window` is deleted. Token sheet §1 is empty again, and `Token.Surface` ships as an empty namespace alongside `Text`, `Border`, `Accent`, and `Material`.** | Anthony's ruling on the orphan flagged earlier today. The row lost its consumer the same day it was written — it was authored for "the settings window," settings became a page inside the main window, and nothing replaced it because slice 1's shell draws no color of its own. Keeping it would have made the sheet's only row a value defended by nothing, which is precisely what the lazy rule exists to prevent, and a stale token is invisible in a way a missing one is not: it looks identical to a live one. **The five namespaces stay** even though all five are now empty — they encode §14.2's closed role set, so deleting `Surface` with its row would make a sixth namespace easier to add rather than harder, which is the opposite of what the encoding is for. The first genuine consumer should be slice 3's waveform, the one element §14.3 already names as having no system precedent. | The two 2026-08-15 entries below that added the row and then flagged it as an orphan. Token sheet §1 returns to "**Empty.** No feature has been built" | No |
| 2026-08-15 | 1 | **The Chat/Audio pill is the custom two-button matched-geometry control after all, and the `Picker(.segmented)` decision taken earlier today is reversed.** An `HStack` of two plain `Button`s inside a `Capsule` well, with one always-present `Capsule` that adopts the selected segment's geometry via `matchedGeometryEffect` and animates on `.snappy`. Fills are `.tertiary` on `.quaternary` — hierarchical system styles, so **no tier-2 token and no authored colour**. Two authored measures, both geometry: 6 pt vertical inset on the label, 2 pt between the capsule and the well. | Anthony asked for Find My's picker specifically, including the way the grey capsule slides between segments. **The system control was built first and screenshotted, and it fails on one thing that is not reachable through its API: the selected label's colour.** `.controlSize(.large)` gives the capsule bezel and `.tint(Color(nsColor: .quaternaryLabelColor))` gives Find My's neutral grey, but AppKit picks the label colour for contrast against the tint — a capsule light enough to read as selected forces a *dark* label, and Find My keeps both labels light. Selected-dark next to unselected-light stops reading as one row of text, which is most of what makes Find My's picker look calm. So this is a capability gap, not a preference for hand-rolling, and the earlier entry's "three tier-2 debts" objection turned out to cost zero: the hierarchical shape styles supply the two fills and adapt to appearance, accent, and increased contrast on their own. **The slide had to be built, not inherited** — I could not confirm the system control animates it, and the obvious SwiftUI form (a conditional capsule inside each segment) does *not* animate: verified by stretching the animation to 20 s and screenshotting, where the capsule jumped. The always-present capsule with `isSource: false` does animate; same test, capsule caught mid-flight. **Consequence for the token sheet:** the "Segmented pill selection" motion row (§5, slice 6) has a consumer again, and it resolves to `.snappy` — a system spring preset, so tier 1. | The `Picker(.segmented)` entry immediately below, from earlier the same day. **§10.2's** original note is restored, not superseded — the custom control it predicted is what shipped | No |
| 2026-08-15 | 1 | **Settings has five sections, not four: General · Dictation · Chat · Models · MCPs. There is no Profiles section — profiles move inside Dictation.** Dictation owns everything §8.1 lists; Chat owns §8.2's per-model overrides and has no profile concept at all. | Anthony: *"instead of having a Profiles section in settings you have a Chat setting and a Dictation setting."* §8 already opens with "two independent scopes, no overlap" and then names them by mechanism — *STT profiles* and *chat model settings*. Naming them by what the user is doing puts the two scopes at the same level in the sidebar, where the old list had one scope visible as **Profiles** and the other invisible, folded into **Models**. Profiles lose nothing: they are the whole of the dictation scope, so they are a list inside Dictation rather than a sibling of it. **What this costs is a boundary that now has to be held:** Models is the §7 catalogue — what is installed, what it weighs, what it can do — and Chat is §8.2's per-model overrides, so "which model chats" and "how that model chats" sit in two sections. Slice 8 builds Models and is where that seam gets tested. | **§8.1's** standing as a top-level settings section, and the settings-sidebar list slice 1 shipped this morning. §8.1's contents are unchanged | No |
| 2026-08-15 | 1 | **§10.2's segmented pill is the system segmented control at `.controlSize(.large)`, and the custom two-button matched-geometry control is not built.** One modifier is the entire change: macOS 26 draws the capsule bezel, the selection material, and the selection transition. Find My's sidebar control is the reference Anthony named. | Anthony asked for the Find My look — glass, pill-shaped — and the system already draws exactly that; the default control size is what was producing the small rounded-rect with a filled selection. The spec's objection to `.segmented` was that it "handles icon-plus-label poorly," and it does, but **§10.2's pill is text-only in both segments**, so the objection never applied to this control. Hand-rolling it would author a radius, a fill, and a selection material that each stop matching macOS on the next release — three tier-2 debts to reproduce something the system ships. **One difference from Find My survives and is deliberate:** Find My's selection is neutral, Sotto's takes the user's accent, because that is the control's default and §8.5 inherits appearance wholesale. `.tint(.secondary)` neutralises it if Anthony wants the reference matched exactly. **Consequence for the token sheet:** the "Segmented pill selection" motion row (§5, slice 6) has no consumer — the system owns the transition — so it should come out rather than be filled in. | **§10.2's** note ("expect a custom control: two buttons in an `HStack` over a rounded container with a matched-geometry selection pill") and the **build order's slice 6** bullet repeating it. Also pulls the pill's design from slice 6 into slice 1 | No |
| 2026-08-15 | 1 | **The main window keeps SwiftUI's toolbar and the system sidebar toggle it draws. `.sidebarTrackingSeparator` is not used, and neither is `.toolbar(removing: .sidebarToggle)`.** The shape that was asked for — sidebar edge to edge, traffic lights over it, no titlebar band — comes from `.fullSizeContentView` plus `titlebarAppearsTransparent` and a hidden title, with `NavigationSplitView` as the content. Nothing about the drawn result changes. | Built and screenshotted both ways. The no-toolbar entry below rests on AppKit's rule that a single toolbar item makes the system redraw the titlebar band; **under SwiftUI's `NavigationSplitView` on macOS 26 that does not hold.** A toolbar exists — `AXToolbar` is present on the window — the system draws the sidebar toggle into it, and no band appears. `.toolbar(removing: .sidebarToggle)` did **not** remove that toggle, so applying it and authoring a replacement produced two toggles side by side; both the modifier and the authored button were deleted. `.sidebarTrackingSeparator` is a toolbar *item* identifier for tracking a divider, and there is no divider to track. **The collapsed case comes free and matches the entry below exactly:** collapsing the sidebar moves the system toggle to the window's top-left past the traffic lights, in a glass capsule, with no code. Roughly forty lines of authored chrome — a titlebar strip, a toggle, a collapsed-toggle overlay, and a `WindowChrome` struct deriving titlebar height and traffic-light inset — were written, screenshotted against the system's own, and removed. | The **build instruction** in the no-toolbar entry below ("the toolbar has to go entirely", "build it with the system `.sidebarTrackingSeparator`"). Its stated shape is unchanged and shipped | No |
| 2026-08-15 | 1 | **Slice 1 ships consuming no tokens, which leaves `surface.window` an orphaned row.** The shell names no color, no font, and no metric anywhere. Flagged rather than deleted — the row was put there by a logged decision, so removing it is Anthony's call. | The lazy rule says a row without a consumer does not belong in the sheet yet, and this one lost its consumer the same day it was written: it was authored for "the settings window", and settings became a page inside the main window. Nothing replaced it, because everything the shell draws is a system component that already resolves to the right semantic value — `NavigationSplitView`'s sidebar, `List(.sidebar)`, `Picker(.segmented)`, `ContentUnavailableView`, `Divider`. **That is the intended outcome, not a gap:** slice 1 was the test of whether the token layer could stay empty under real pressure, and the answer is that a shell built out of system components generates no rows at all. The first genuine consumers should be slice 3's waveform, which §14.3 already names as the one element with no system precedent. | The third 2026-08-15 entry below, which names the settings window as `surface.window`'s first consumer | No |
| 2026-08-15 | 1 | **Settings is a page inside the main window, not a separate window, and `Cmd+,` toggles it.** Clicking the settings button bottom-left, or pressing `Cmd+,`, replaces the main window's content with the settings page; pressing `Cmd+,` again returns to exactly where the user was. In settings the sidebar swaps its Chat/Audio pill for a list of settings sections, and its bottom row becomes a back arrow labelled **Back**. `Cmd+,` from the overlay opens the same page. **Logged retroactively** — the decision was made in session `df1f1c06` on 2026-08-15 and never recorded. | Anthony's own words: *"The main window is not a separate window — turns into the settings page… It's not like a separate window, but it is still command plus comma."* The reference is Claude Desktop and t3.chat, which both do this. What loses is the conventional macOS settings window; what it costs is that Sotto's settings cannot sit beside the main window, and Anthony accepted that explicitly. Toggling rather than opening is what makes it navigation rather than a mode. | **§10.2**, which says "Settings open separately via `Cmd+,`" — now wrong. §8.3 is unaffected: it only rules out `Cmd+.` | No |
| 2026-08-15 | 1 | **The main window has no toolbar. The sidebar runs full height, the traffic lights sit over it, and the Chat/Audio pill lives at the sidebar's top-left.** The sidebar toggle sits on the sidebar; collapsing the sidebar moves it to the window's top-left past the traffic lights. Safari is the named reference. **Logged retroactively** — same session, never recorded. | Anthony: *"I liked it better when the sidebar took up the entire left side… That means that the window control buttons will be on the sidebar."* This is the standard macOS shape (Safari, Finder, Mail, Notes), not a custom one. It has a hard consequence worth recording: **a single toolbar item makes the system draw the titlebar band back across the top**, so the toolbar has to go entirely rather than be emptied — which is what removes §10.2's home for the segmented pill and forces it into the sidebar. Build it with the system `.sidebarTrackingSeparator` and `.fullSizeContentView`; see `.claude/rules/design.md` §7. | **§10.2**, which places the segmented pill "in the toolbar" and assumes a toolbar exists | No |
| 2026-08-15 | 1 | **First tier-1 token row: `surface.window` → `NSColor.windowBackgroundColor`, first consumer the settings window.** The token layer is `Sotto/Design/Token.swift` — `enum Token` with nested `Surface`, `Text`, `Border`, `Accent`, `Material` namespaces, tier 2 present as an empty `Token.Authored` and nothing else. It also carries one geometry helper, `Token.shape(radius:)`, returning `.rect(cornerRadius:style:.continuous)`. `Text`, `Border`, `Accent`, and `Material` ship empty. | The row earns itself: `.windowBackgroundColor` picks up the macOS 26 window-background wallpaper tint, which a drawn fill would not, and the settings window is built this slice. The four empty namespaces are the closed role set §14.2 names, encoded so a sixth namespace has to be argued for rather than added. `shape(radius:)` exists because `RoundedRectangle(cornerRadius:)` defaults to a circular arc and §14.1's continuous-corner rule is the one most likely to be broken by a call site written in a hurry — the radius stays a parameter because no radius has a consumer yet and `radius.surface` is still open (tokens §4). | Token sheet §1, which reads "**Empty.** No feature has been built" and now has one row. §2 is still genuinely empty | No |
| 2026-08-15 | 1 | **Every view is SwiftUI. AppKit is confined to three jobs: the app delegate, the status item and its `NSMenu`, and the `NSWindow`/`NSPanel` objects that host SwiftUI through `NSHostingView`.** The main window is SwiftUI as well, not only the floating surfaces. The Xcode project is created from the AppKit (XIB) template accordingly. | Pure SwiftUI is not available, so the real choice was where the seam falls rather than whether there is one. Three requirements have no SwiftUI form: the HUD must be a **non-activating** `NSPanel`, because it appears while the user is dictating into another app and a panel that takes key status destroys the focused element the AX write targets — the failure is silent, and every dictation degrades to the clipboard path (§3, §4.5); the event tap is `CGEventTap` on a dedicated thread (§2.5); insertion is `AXUIElement` (§3). The SwiftUI `App` lifecycle would therefore not remove the split but scatter it — `NSApplicationDelegateAdaptor` to recover the delegate, launch-window suppression, manual activation-policy calls fighting scene management — and still require `NSPanel` for the HUD and overlay. **What decides the remaining question is §12's own objection**: an AppKit main window beside SwiftUI floating surfaces would force every §14.2 role to carry both a `Color` and an `NSColor` form, two definitions of one role free to drift. Pushing the main window into SwiftUI answers it — one `Color` per token, and the AppKit layer draws nothing at all. | §12 open issue 2 — resolves it. Also settles the §14.2 token-form question the issue raised | No |
